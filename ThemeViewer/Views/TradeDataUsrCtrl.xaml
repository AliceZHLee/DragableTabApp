<UserControl x:Class="ThemeViewer.Views.TradeDataUsrCtrl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ThemeViewer.Models"
             mc:Ignorable="d" 
             d:DesignWidth="600" >
    <UserControl.Resources>
        <Style x:Key="CustomerItemStyle" TargetType="{x:Type DataGridRow}">
            <Setter Property="HorizontalContentAlignment" Value="Stretch" />
        </Style>
        <Style x:Key="SortDirectionStyle" TargetType="{x:Type TextBlock}">
            <Setter Property="FontFamily" Value="{StaticResource FontAwesome}"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>

        <Style TargetType="{x:Type ListBox}" x:Key="FilterListbox_Style">
            <Setter Property="Height" Value="180"/>
            <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Auto" />
            <Setter Property="ItemTemplate">
                <Setter.Value>
                    <DataTemplate>
                        <CheckBox IsChecked="{Binding IsChecked}" Content="{Binding Label}" VerticalAlignment="Stretch" Checked="CheckBox_Update" Unchecked="CheckBox_Update"/>
                    </DataTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <local:TextFltOptions x:Key="ComboBoxOptions"/>
    </UserControl.Resources>

    <Grid Margin="5">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border BorderBrush="Black" Background="White" Grid.Row="0">
            <Grid Margin="0,5,0,5">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0" Grid.ColumnSpan="2" Orientation="Horizontal" x:Name="current_selected_FilterDic" CanHorizontallyScroll="True" VerticalAlignment="Center">
                    <TextBlock Grid.Column="0" Text="&#xf0b0;" FontSize="16" FontFamily="{StaticResource FontAwesome}" Margin="5"  HorizontalAlignment="Left" />
                    <StackPanel Grid.Column="1" Orientation="Horizontal" FlowDirection="LeftToRight"  VerticalAlignment="Center">
                        <TextBlock Name="Default_Filter_Label" Text="No filters applied. Click on a column header to set one" VerticalAlignment="center"/>
                        <ItemsControl ItemsSource="{Binding Path=FilterLabel}" HorizontalAlignment="Stretch" Name="added_FilterDic_Label" Visibility="Hidden">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <StackPanel Orientation="Horizontal"/>
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border BorderBrush="Gray" BorderThickness="1" CornerRadius="4" Margin="4">
                                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Stretch" >
                                            <TextBlock Background="DarkBlue" FontWeight="Bold" Foreground="white" FontSize="14" Text="{Binding Path=Label}" Padding="2" VerticalAlignment="Center" Margin="0,0,3,0" />
                                            <TextBlock Background="White" Foreground="Black" Text="{Binding Path=SelectedFilterDic}" VerticalAlignment="Center" Margin="2,0,1,0" /> 
                                            <Button Click="CloseFltLabel_Click" Tag="{Binding Path=Label}" Style="{StaticResource labelBtnTemplate}" Grid.Column="1" Margin="4" Width="12" DockPanel.Dock="Right" HorizontalAlignment="Right"/>
                                        </StackPanel>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                </StackPanel>
                <Button Grid.Column="2" Margin="5" HorizontalAlignment="Right" Background="Transparent" Padding="2" BorderBrush="Transparent" Click="ExportToExcel">
                    <!--Command="{Binding Path=ExportCommand}" CommandParameter="{Binding ElementName=dataGrid}">-->
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="5"/>
                        </Style>
                    </Button.Resources>
                    <Button.Content>
                        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Orientation="Horizontal" Background="Transparent" Height="25">
                            <TextBlock x:Name="download_btn" Text="&#xf019;" FontFamily="{StaticResource FontAwesome}" VerticalAlignment="Center" Background="Transparent"/>
                            <TextBlock Text="Download" VerticalAlignment="Center" HorizontalAlignment="Center" Margin="2 1" Padding="5" FontWeight="Bold"/>
                        </StackPanel>
                    </Button.Content>
                </Button>
                <Button Grid.Column="3" Name="CustomizeBtn" Margin="5" HorizontalAlignment="Right" Background="Transparent" Padding="2" BorderBrush="Transparent" Click="Customize_Click">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="5"/>
                        </Style>
                    </Button.Resources>
                    <Button.Content>
                        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Orientation="Horizontal" Background="Transparent" Height="25">
                            <TextBlock x:Name="Customize_btn" Text="&#xf013;" FontFamily="{StaticResource FontAwesome}" VerticalAlignment="Center" Background="Transparent"/>
                            <TextBlock Text="Customize" VerticalAlignment="Center" HorizontalAlignment="Center" Margin="2 1" Padding="5" FontWeight="Bold"/>
                        </StackPanel>
                    </Button.Content>
                </Button>
                <Popup Name="pop_CustomizeColumn" Closed="CustomizePopUpClose" PlacementTarget="{Binding ElementName=CustomizeBtn}" Style="{Binding Source={StaticResource PopUpStyle}}">
                    <Border Background="White" BorderBrush="Gray" BorderThickness="1">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition />
                                <RowDefinition />
                                <RowDefinition />
                            </Grid.RowDefinitions>
                            <StackPanel Orientation="Vertical" Grid.Row="0" Grid.RowSpan="2">
                                <CheckBox Name="checkbox_selectAll_CustomizeColumns" Grid.Row="0" Margin="10,10,0,0" IsChecked="True" Checked="SelectAllCheckBox_Checked" Unchecked="SelectAllCheckBox_Checked" Content="(Select All)" />
                                <ListBox Name="lstColumnOptions" Grid.Row="1" Margin="10" ItemsSource="{Binding TradeColumns}" Style="{StaticResource FilterListbox_Style}" Height="500"/>
                            </StackPanel>
                            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center">
                                <Button Margin="10" Padding="5" Content="CANCEL" Click="ClosePopUp_Click" Style="{StaticResource cancelBtn}"/>
                                <Button Padding="5" Margin="10" Background="#0094B3" Foreground="White" Content="UPDATE" Click="UpdateColumn_Click" Style="{StaticResource popUpBtn}"/>
                            </StackPanel>
                        </Grid>
                    </Border>
                </Popup>

                <Button Grid.Column="4"  Margin="5" HorizontalAlignment="Right" Background="Transparent" Padding="2" BorderBrush="Transparent"
                        Click="Reset_CurrFlts">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="5"/>
                        </Style>
                    </Button.Resources>
                    <Button.Content>
                        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Orientation="Horizontal" Background="Transparent" Height="25">
                            <TextBlock x:Name="resetFlt_btn" Text="&#xf2f9;" FontFamily="{StaticResource FontAwesome}" VerticalAlignment="Center" Background="Transparent"/>
                            <TextBlock Text="Reset Filters" VerticalAlignment="Center" HorizontalAlignment="Center" Margin="2 1" Padding="5" FontWeight="Bold"/>
                        </StackPanel>
                    </Button.Content>
                </Button>
            </Grid>
        </Border>
        <DockPanel Grid.Row="1">
            <DataGrid x:Name="dataGrid" DataContext="{Binding TradeDataViewS}" ItemsSource="{Binding}" AutoGenerateColumns="False" IsReadOnly="True"                       
                       HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto" RowBackground="Transparent"
                       LostFocus="LostFocus_datagrid" SelectionMode="Extended">
                <DataGrid.Resources>
                    <SolidColorBrush x:Key="{x:Static SystemColors.HighlightBrushKey}" Color="#3d8cf8"/>
                </DataGrid.Resources>
                <DataGrid.Background>
                    <ImageBrush ImageSource="\Resources\ThemeInternationalTrd.png" Stretch="None" Opacity="0.2" />
                </DataGrid.Background>
                <DataGrid.Columns>
                    <DataGridTextColumn Width="auto" Binding="{Binding Path=TradeID}">
                        <DataGridTextColumn.HeaderStyle>
                            <Style TargetType="{x:Type DataGridColumnHeader}">
                                <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                            </Style>
                        </DataGridTextColumn.HeaderStyle>
                        <DataGridTextColumn.Header>
                            <DockPanel>
                                <TextBlock Style="{StaticResource SortDirectionStyle}">
                                    <TextBlock.Triggers>
                                    </TextBlock.Triggers>
                                </TextBlock>
                                <TextBlock Text="Trade ID (Leg)" Margin="10,5" />
                                <ToggleButton  Name="TradeID" Style="{Binding Source={StaticResource DropDown_btn}}" Click="BtnFilter_Click"/>
                                <Popup Name="pop_ComboFlt_TradeID" Style="{Binding Source={StaticResource PopUpStyle}}">
                                    <Border Background="White" BorderBrush="Gray" BorderThickness="1">
                                        <Grid>
                                            <Grid.RowDefinitions>
                                                <RowDefinition />
                                                <RowDefinition />
                                                <RowDefinition />
                                                <RowDefinition />
                                                <RowDefinition />
                                            </Grid.RowDefinitions>
                                            <TextBlock Text="VALUE" Margin="10"  VerticalAlignment="Center" FontWeight="Bold"/>
                                            <ComboBox Name="TextFltOptions_TradeID" Grid.Row="1" Margin="10,0,10,0" SelectedIndex="0" ItemsSource="{StaticResource ComboBoxOptions}"/>
                                            <TextBlock Grid.Row="2" Text="TEXT" Margin="10" DockPanel.Dock="Left" VerticalAlignment="Center" FontWeight="Bold"/>
                                            <TextBox Name="TradeID_Flt_Input"  Grid.Row="3" Margin="10,0,10,0"/>

                                            <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Center">
                                                <Button Margin="10" Padding="5" Content="CANCEL" Click="ClosePopUp_Click" Style="{StaticResource cancelBtn}"/>
                                                <Button Padding="5" Margin="10" Background="#0094B3" Foreground="White" Content="APPLY" Style="{StaticResource popUpBtn}"
                                                    Click="TrdID_Filter_Apply"/>
                                            </StackPanel>
                                        </Grid>
                                    </Border>
                                </Popup>
                            </DockPanel>
                        </DataGridTextColumn.Header>

                    </DataGridTextColumn>
                    <DataGridTextColumn Width="auto" Binding="{Binding Path=DealID}">
                        <DataGridTextColumn.HeaderStyle>
                            <Style TargetType="{x:Type DataGridColumnHeader}">
                                <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                            </Style>
                        </DataGridTextColumn.HeaderStyle>
                        <DataGridTextColumn.Header>
                            <DockPanel>
                                <TextBlock Style="{StaticResource SortDirectionStyle}">
                                    <TextBlock.Triggers>
                                    </TextBlock.Triggers>
                                </TextBlock>
                                <TextBlock Text="Deal ID" Margin="10,5" />
                                <ToggleButton Name="DealID" Style="{Binding Source={StaticResource DropDown_btn}}" Click="BtnFilter_Click"/>
                                <Popup Name="pop_ComboFlt_DealID" Style="{Binding Source={StaticResource PopUpStyle}}">
                                    <Border Background="White" BorderBrush="Gray" BorderThickness="1">

                                        <Grid>
                                            <Grid.RowDefinitions>
                                                <RowDefinition />
                                                <RowDefinition />
                                                <RowDefinition />
                                                <RowDefinition />
                                                <RowDefinition />
                                            </Grid.RowDefinitions>
                                            <TextBlock Text="VALUE" Margin="10"  VerticalAlignment="Center" FontWeight="Bold"/>
                                            <ComboBox Name="TextFltOptions_DealID" Grid.Row="1" Margin="10,0,10,0" SelectedIndex="0" ItemsSource="{StaticResource ComboBoxOptions}"/>
                                            <TextBlock Grid.Row="2" Text="TEXT" Margin="10" DockPanel.Dock="Left" VerticalAlignment="Center" FontWeight="Bold"/>
                                            <TextBox Name="DealID_Flt_Input"  Grid.Row="3" Margin="10,0,10,0"/>

                                            <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Center">
                                                <Button Margin="10" Padding="5" Content="CANCEL" Click="ClosePopUp_Click" Style="{StaticResource cancelBtn}" />
                                                <Button Padding="5" Margin="10" Background="#0094B3" Foreground="White" Content="APPLY" Style="{StaticResource popUpBtn}"
                                                    Click="DealID_Filter_Apply"/>
                                            </StackPanel>
                                        </Grid>
                                    </Border>
                                </Popup>
                            </DockPanel>
                        </DataGridTextColumn.Header>
                    </DataGridTextColumn>
                    <DataGridTextColumn  Width="auto" Binding="{Binding Path=SubmittedBy}">
                        <DataGridTextColumn.HeaderStyle>
                            <Style TargetType="{x:Type DataGridColumnHeader}">
                                <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                            </Style>
                        </DataGridTextColumn.HeaderStyle>
                        <DataGridTextColumn.Header>
                            <DockPanel>
                                <TextBlock Style="{StaticResource SortDirectionStyle}">
                                    <TextBlock.Triggers>
                                    </TextBlock.Triggers>
                                </TextBlock>
                                <TextBlock Text="Submitted By" Margin="10,5" />
                                <ToggleButton Name="SubmittedBy" Click="BtnFilter_Click" Style="{Binding Source={StaticResource DropDown_btn}}"/>
                                <Popup Name="pop_CheckboxFlt_SubmittedBy" Closed="ClosePopUp" Style="{Binding Source={StaticResource PopUpStyle}}">
                                    <Border Background="White" BorderBrush="Gray" BorderThickness="1">
                                        <Grid>
                                            <Grid.RowDefinitions>
                                                <RowDefinition />
                                                <RowDefinition />
                                                <RowDefinition />
                                                <RowDefinition />
                                            </Grid.RowDefinitions>
                                            <DockPanel Grid.Row="0" Margin="10">
                                                <!--<TextBlock Text="SEARCH" Margin="10" DockPanel.Dock="Left" VerticalAlignment="Center" FontWeight="Bold"/>
                                                <TextBox x:Name="tbFilter_SubmittedBy" Margin="10" Tag="SubmittedBy" Width="Auto"/>-->
                                            </DockPanel>
                                            <StackPanel Orientation="Vertical" Grid.Row="1" Grid.RowSpan="2">
                                                <CheckBox Grid.Row="1" Name="Checkbox_selectAll_SubmittedBy" Margin="10,0,0,0" Tag="{Binding}"  IsChecked="True" Checked="SelectAllCheckBox_Checked" Unchecked="SelectAllCheckBox_Checked">(Select All)</CheckBox>
                                                <ListBox Grid.Row="2" Margin="10" ItemsSource="{Binding}" Style="{Binding Source={StaticResource FilterListbox_Style}}" />
                                            </StackPanel>
                                            <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Center">
                                                <Button Margin="10" Padding="5" Content="CANCEL" Click="ClosePopUp_Click"  Style="{StaticResource cancelBtn}" />
                                                <Button Padding="5" Margin="10" Background="#0094B3" Foreground="White" Content="APPLY" Style="{StaticResource popUpBtn}"
                                                    Click="SubmittedBy_Filter_Apply"/>
                                            </StackPanel>
                                        </Grid>
                                    </Border>
                                </Popup>
                            </DockPanel>
                        </DataGridTextColumn.Header>

                    </DataGridTextColumn>
                    <DataGridTextColumn  Width="auto" Binding="{Binding Path=TradeRegistrationDateTime,StringFormat=dd/MM/yyyy hh:mm tt}">
                        <DataGridTextColumn.HeaderStyle>
                            <Style TargetType="{x:Type DataGridColumnHeader}">
                                <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                            </Style>
                        </DataGridTextColumn.HeaderStyle>
                        <DataGridTextColumn.Header>
                            <DockPanel>
                                <TextBlock Style="{StaticResource SortDirectionStyle}">
                                    <TextBlock.Triggers>

                                    </TextBlock.Triggers>
                                </TextBlock>
                                <TextBlock Text="Trade Registration Date &amp; Time (SGT)" Margin="10,5" />
                                <ToggleButton Name="TradeRegistrationDateTime" Click="BtnFilter_Click" Style="{Binding Source={StaticResource DropDown_btn}}"/>
                                <Popup Name="pop_DateTimeFlt_TrdRegistration" Style="{Binding Source={StaticResource PopUpStyle}}">
                                    <Border Background="White" BorderBrush="Gray" BorderThickness="1">
                                        <Grid>
                                            <Grid.RowDefinitions>
                                                <RowDefinition Height="auto" />
                                                <RowDefinition Height="auto" />
                                                <RowDefinition Height="auto" />
                                                <RowDefinition Height="auto" />
                                                <RowDefinition Height="auto" />
                                            </Grid.RowDefinitions>
                                            <TextBlock  Grid.Row="0" Margin="10" Text="FROM"  VerticalAlignment="Center" FontWeight="Bold"/>
                                            <DatePicker Grid.Row="1" Margin="10,0,10,0" Name="datePicker_TrdRegistFrom" 
                                                    SelectedDateFormat="Long" CalendarStyle="{StaticResource styleCalendar}"/>

                                            <TextBlock  Grid.Row="2" Margin="10" Text="TO" VerticalAlignment="Center" FontWeight="Bold"/>
                                            <DatePicker Grid.Row="3" Margin="10,0,10,5" Name="datePicker_TrdRegistTo" 
                                                    SelectedDateFormat="Long" CalendarStyle="{StaticResource styleCalendar}"/>

                                            <StackPanel Grid.Row="4"  Orientation="Horizontal" HorizontalAlignment="Center">
                                                <Button Margin="10" Padding="5" Content="CANCEL" Click="ClosePopUp_Click"  Style="{StaticResource cancelBtn}"/>
                                                <Button Margin="10" Padding="5" Background="#0094B3" Foreground="White" Content="APPLY"  Style="{StaticResource popUpBtn}"
                                                     Click="TrdRegistDT_Filter_Apply"/>
                                            </StackPanel>
                                        </Grid>
                                    </Border>
                                </Popup>
                            </DockPanel>
                        </DataGridTextColumn.Header>
                    </DataGridTextColumn>
                    <DataGridTextColumn  Width="auto" Binding="{Binding Path=MarketSegmentID}">
                        <DataGridTextColumn.HeaderStyle>
                            <Style TargetType="{x:Type DataGridColumnHeader}">
                                <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                            </Style>
                        </DataGridTextColumn.HeaderStyle>
                        <DataGridTextColumn.Header>
                            <DockPanel>
                                <TextBlock Style="{StaticResource SortDirectionStyle}">
                                    <TextBlock.Triggers>
                                    </TextBlock.Triggers>
                                </TextBlock>
                                <TextBlock Text="Market" Margin="10,5" />
                                <ToggleButton Name="MarketSegmentID" Click="BtnFilter_Click" Style="{Binding Source={StaticResource DropDown_btn}}" />
                                <Popup Name="pop_CheckboxFlt_MarketSegmentID" Closed="ClosePopUp" Style="{Binding Source={StaticResource PopUpStyle}}">
                                    <Border Background="White" BorderBrush="Gray" BorderThickness="1">
                                        <Grid>
                                            <Grid.RowDefinitions>
                                                <RowDefinition />
                                                <RowDefinition />
                                                <RowDefinition />
                                                <RowDefinition />
                                            </Grid.RowDefinitions>
                                            <DockPanel Grid.Row="0" Margin="10">
                                                <!--<TextBlock Text="SEARCH" Margin="10" DockPanel.Dock="Left" VerticalAlignment="Center" FontWeight="Bold"/>
                                                <TextBox x:Name="tbFilter_MarketSegmentID" Margin="10" Tag="MarketSegmentID" Width="Auto"/>-->
                                            </DockPanel>
                                            <StackPanel Orientation="Vertical" Grid.Row="1" Grid.RowSpan="2">
                                                <CheckBox Grid.Row="1" Name="Checkbox_selectAll_MarketSegmentID" Margin="10,0,0,0" Tag="{Binding}"  IsChecked="True" Checked="SelectAllCheckBox_Checked" Unchecked="SelectAllCheckBox_Checked">(Select All)</CheckBox>
                                                <ListBox Grid.Row="2" Margin="10" ItemsSource="{Binding}" Style="{Binding Source={StaticResource FilterListbox_Style}}" />
                                            </StackPanel>
                                            <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Center">
                                                <Button Margin="10" Padding="5" Content="CANCEL" Click="ClosePopUp_Click" Style="{StaticResource cancelBtn}"/>
                                                <Button Padding="5" Margin="10" Background="#0094B3" Foreground="White" Content="APPLY" Style="{StaticResource popUpBtn}"
                                                    Click="MarketSegID_Filter_Apply"/>
                                            </StackPanel>
                                        </Grid>
                                    </Border>
                                </Popup>
                            </DockPanel>
                        </DataGridTextColumn.Header>
                    </DataGridTextColumn>
                    <DataGridTextColumn Width="auto" Binding="{Binding Path=SecurityID}">
                        <DataGridTextColumn.HeaderStyle>
                            <Style TargetType="{x:Type DataGridColumnHeader}">
                                <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                            </Style>
                        </DataGridTextColumn.HeaderStyle>
                        <DataGridTextColumn.Header>
                            <DockPanel>
                                <TextBlock Style="{StaticResource SortDirectionStyle}">
                                    <TextBlock.Triggers>
                                    </TextBlock.Triggers>
                                </TextBlock>
                                <TextBlock Text="Security" Margin="10,5" />
                                <ToggleButton  Name="SecurityID" Style="{StaticResource DropDown_btn}" Click="BtnFilter_Click"/>
                                <Popup Name="pop_ComboFlt_SecurityID" Style="{Binding Source={StaticResource PopUpStyle}}">
                                    <Border Background="White" BorderBrush="Gray" BorderThickness="1">
                                        <Grid>
                                            <Grid.RowDefinitions>
                                                <RowDefinition />
                                                <RowDefinition />
                                                <RowDefinition />
                                                <RowDefinition />
                                                <RowDefinition />
                                            </Grid.RowDefinitions>
                                            <TextBlock Text="VALUE" Margin="10"  VerticalAlignment="Center" FontWeight="Bold"/>
                                            <ComboBox Name="TextFltOptions_SecurityID" Grid.Row="1" Margin="10,0,10,0" SelectedIndex="0" ItemsSource="{StaticResource ComboBoxOptions}"/>
                                            <TextBlock Grid.Row="2" Text="TEXT" Margin="10" DockPanel.Dock="Left" VerticalAlignment="Center" FontWeight="Bold"/>
                                            <TextBox Name="SecurityID_Flt_Input"  Grid.Row="3" Margin="10,0,10,0"/>

                                            <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Center">
                                                <Button Margin="10" Padding="5" Content="CANCEL" Click="ClosePopUp_Click" Style="{StaticResource cancelBtn}" />
                                                <Button Padding="5" Margin="10" Background="#0094B3" Foreground="White" Content="APPLY" Style="{StaticResource popUpBtn}"
                                                    Click="SecurityID_Filter_Apply"/>
                                            </StackPanel>
                                        </Grid>
                                    </Border>
                                </Popup>
                            </DockPanel>
                        </DataGridTextColumn.Header>
                    </DataGridTextColumn>
                    <DataGridTextColumn  Width="auto" Binding="{Binding Path=Instrument}">
                        <DataGridTextColumn.HeaderStyle>
                            <Style TargetType="{x:Type DataGridColumnHeader}">
                                <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                            </Style>
                        </DataGridTextColumn.HeaderStyle>
                        <DataGridTextColumn.Header>
                            <DockPanel>
                                <TextBlock Style="{StaticResource SortDirectionStyle}">
                                    <TextBlock.Triggers>
                                    </TextBlock.Triggers>
                                </TextBlock>
                                <TextBlock Text="Instrument" Margin="10,5" />
                                <ToggleButton Name="Instrument" Click="BtnFilter_Click" Style="{Binding Source={StaticResource DropDown_btn}}" />
                                <Popup Name="pop_CheckboxFlt_Instrument" Closed="ClosePopUp" Style="{Binding Source={StaticResource PopUpStyle}}">
                                    <Border Background="White" BorderBrush="Gray" BorderThickness="1">
                                        <Grid>
                                            <Grid.RowDefinitions>
                                                <RowDefinition />
                                                <RowDefinition />
                                                <RowDefinition />
                                                <RowDefinition />
                                            </Grid.RowDefinitions>
                                            <DockPanel Grid.Row="0" Margin="10">
                                                <!--<TextBlock Text="SEARCH" Margin="10" DockPanel.Dock="Left" VerticalAlignment="Center" FontWeight="Bold"/>
                                                <TextBox x:Name="tbFilter_Instrument" Margin="10" Tag="Instrument" Width="Auto"/>-->
                                            </DockPanel>
                                            <StackPanel Orientation="Vertical" Grid.Row="1" Grid.RowSpan="2">
                                                <CheckBox Grid.Row="1" Name="Checkbox_selectAll_Instrument" Margin="10,0,0,0" Tag="{Binding}"  IsChecked="True" Checked="SelectAllCheckBox_Checked" Unchecked="SelectAllCheckBox_Checked">(Select All)</CheckBox>
                                                <ListBox Grid.Row="2" Margin="10" ItemsSource="{Binding}" Style="{Binding Source={StaticResource FilterListbox_Style}}" />
                                            </StackPanel>
                                            <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Center">
                                                <Button Margin="10" Padding="5" Content="CANCEL" Click="ClosePopUp_Click" Style="{StaticResource cancelBtn}"/>
                                                <Button Padding="5" Margin="10" Background="#0094B3" Foreground="White" Content="APPLY"  Style="{StaticResource popUpBtn}"
                                                    Click="Instrument_Filter_Apply"/>
                                            </StackPanel>
                                        </Grid>
                                    </Border>
                                </Popup>
                            </DockPanel>
                        </DataGridTextColumn.Header>
                    </DataGridTextColumn>
                    <DataGridTextColumn  Width="auto" Binding="{Binding Path=TrdType}">
                        <DataGridTextColumn.HeaderStyle>
                            <Style TargetType="{x:Type DataGridColumnHeader}">
                                <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                            </Style>
                        </DataGridTextColumn.HeaderStyle>
                        <DataGridTextColumn.Header>
                            <DockPanel>
                                <TextBlock Style="{StaticResource SortDirectionStyle}">
                                    <TextBlock.Triggers>
                                    </TextBlock.Triggers>
                                </TextBlock>
                                <TextBlock Text="Trade Type" Margin="10,5" />
                                <ToggleButton Name="TrdType" Click="BtnFilter_Click" Style="{Binding Source={StaticResource DropDown_btn}}" />
                                <Popup Name="pop_CheckboxFlt_TrdType" Closed="ClosePopUp" Style="{Binding Source={StaticResource PopUpStyle}}">
                                    <Border Background="White" BorderBrush="Gray" BorderThickness="1">
                                        <Grid>
                                            <Grid.RowDefinitions>
                                                <RowDefinition />
                                                <RowDefinition />
                                                <RowDefinition />
                                                <RowDefinition />
                                            </Grid.RowDefinitions>
                                            <DockPanel Grid.Row="0" Margin="10">
                                                <!--<TextBlock Text="SEARCH" Margin="10" DockPanel.Dock="Left" VerticalAlignment="Center" FontWeight="Bold"/>
                                                <TextBox x:Name="tbFilter_TrdType" Margin="10" Tag="TrdType" Width="Auto"/>-->
                                            </DockPanel>
                                            <StackPanel Orientation="Vertical" Grid.Row="1" Grid.RowSpan="2">
                                                <CheckBox Grid.Row="1" Name="Checkbox_selectAll_TrdType" Margin="10,0,0,0" Tag="{Binding}"  IsChecked="True" Checked="SelectAllCheckBox_Checked" Unchecked="SelectAllCheckBox_Checked">(Select All)</CheckBox>
                                                <ListBox Grid.Row="2" Margin="10" ItemsSource="{Binding}" Style="{Binding Source={StaticResource FilterListbox_Style}}" />
                                            </StackPanel>
                                            <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Center">
                                                <Button Margin="10" Padding="5" Content="CANCEL" Click="ClosePopUp_Click" Style="{StaticResource cancelBtn}"/>
                                                <Button Padding="5" Margin="10" Background="#0094B3" Foreground="White" Content="APPLY" Style="{StaticResource popUpBtn}"
                                                    Click="TrdType_Filter_Apply"/>
                                            </StackPanel>
                                        </Grid>
                                    </Border>
                                </Popup>
                            </DockPanel>
                        </DataGridTextColumn.Header>
                    </DataGridTextColumn>
                    <DataGridTextColumn  Width="auto" Binding="{Binding Path=ExpireDate,StringFormat=MMM-yyyy}">
                        <DataGridTextColumn.HeaderStyle>
                            <Style TargetType="{x:Type DataGridColumnHeader}">
                                <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                            </Style>
                        </DataGridTextColumn.HeaderStyle>
                        <DataGridTextColumn.Header>
                            <DockPanel>
                                <TextBlock Style="{StaticResource SortDirectionStyle}">
                                    <TextBlock.Triggers>
                                    </TextBlock.Triggers>
                                </TextBlock>
                                <TextBlock Text="Expiry" Margin="10,5" />
                                <ToggleButton Name="ExpireDate" Click="BtnFilter_Click" Style="{Binding Source={StaticResource DropDown_btn}}"/>
                                <Popup Name="pop_DateTimeFlt_ExpireDate" Style="{Binding Source={StaticResource PopUpStyle}}">
                                    <Border Background="White" BorderBrush="Gray" BorderThickness="1">
                                        <Grid>
                                            <Grid.RowDefinitions>
                                                <RowDefinition Height="auto" />
                                                <RowDefinition Height="auto" />
                                                <RowDefinition Height="auto" />
                                                <RowDefinition Height="auto" />
                                                <RowDefinition Height="auto" />
                                            </Grid.RowDefinitions>
                                            <TextBlock  Grid.Row="0" Margin="10" Text="FROM"  VerticalAlignment="Center" FontWeight="Bold"/>
                                            <DatePicker Grid.Row="1" Margin="10,0,10,0" Name="datePicker_ExpireDateFrom" 
                                                    SelectedDateFormat="Long" CalendarStyle="{StaticResource styleCalendar}"/>

                                            <TextBlock  Grid.Row="2" Margin="10" Text="TO" VerticalAlignment="Center" FontWeight="Bold"/>
                                            <DatePicker Grid.Row="3" Margin="10,0,10,5" Name="datePicker_ExpireDateTo" 
                                                    SelectedDateFormat="Long" CalendarStyle="{StaticResource styleCalendar}"/>

                                            <StackPanel Grid.Row="4"  Orientation="Horizontal" HorizontalAlignment="Center">
                                                <Button Margin="10" Padding="5" Content="CANCEL" Click="ClosePopUp_Click" Style="{StaticResource cancelBtn}"/>
                                                <Button Margin="10" Padding="5" Background="#0094B3" Foreground="White" Content="APPLY" Style="{StaticResource popUpBtn}"
                                                     Click="ExpireDate_Filter_Apply"/>
                                            </StackPanel>
                                        </Grid>
                                    </Border>
                                </Popup>
                            </DockPanel>
                        </DataGridTextColumn.Header>
                    </DataGridTextColumn>
                    <DataGridTextColumn  Width="auto" Binding="{Binding Path=Price}">
                        <DataGridTextColumn.HeaderStyle>
                            <Style TargetType="{x:Type DataGridColumnHeader}">
                                <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                            </Style>
                        </DataGridTextColumn.HeaderStyle>
                        <DataGridTextColumn.Header>
                            <DockPanel>
                                <TextBlock Style="{StaticResource SortDirectionStyle}">
                                    <TextBlock.Triggers></TextBlock.Triggers>
                                </TextBlock>
                                <TextBlock Text="Price" Margin="10,5" />
                                <ToggleButton Name="Price"  Click="BtnFilter_Click" 
                                          Style="{Binding Source={StaticResource DropDown_btn}}"/>
                                <Popup x:Name="pop_RangeFlt_Price" Style="{Binding Source={StaticResource PopUpStyle}}">
                                    <Border Background="White" BorderBrush="Gray" BorderThickness="1">
                                        <Grid>
                                            <Grid.RowDefinitions>
                                                <RowDefinition Height="1*" />
                                                <RowDefinition Height="2*"  />
                                                <RowDefinition Height="1*" />
                                                <RowDefinition Height="2*" />
                                                <RowDefinition Height="1*" />
                                            </Grid.RowDefinitions>

                                            <TextBlock Text="Minimum" Margin="10" Foreground="Black"/>
                                            <Grid Grid.Row="1" VerticalAlignment="Top" HorizontalAlignment="Stretch" Margin="10,0,10,0">
                                                <Grid.RowDefinitions>
                                                    <RowDefinition />
                                                    <RowDefinition />
                                                </Grid.RowDefinitions>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="3*" />
                                                    <ColumnDefinition Width="1*" />
                                                </Grid.ColumnDefinitions>
                                                <TextBox x:Name="MinNum_Price" Text="" Grid.Row="0" Grid.RowSpan="2" HorizontalAlignment="Stretch"/>
                                                <RepeatButton Grid.Column="1" Grid.Row="0" FontSize="8" Content="&#xf0de;" FontFamily="{StaticResource FontAwesome}" 
                                      VerticalContentAlignment="Center" HorizontalContentAlignment="Center" Click="NUDButtonUP_Click" />
                                                <RepeatButton Grid.Column="1" Grid.Row="1" FontSize="8" Content="&#xf0dd;" FontFamily="{StaticResource FontAwesome}" 
                                      VerticalContentAlignment="Center" HorizontalContentAlignment="Center" Height="13" VerticalAlignment="Bottom" Click="NUDButtonDown_Click">
                                                    <RepeatButton.Style>
                                                        <Style TargetType="{x:Type RepeatButton}">
                                                            <Style.Triggers>
                                                                <DataTrigger Binding="{Binding Text, ElementName=MinNum_Price, UpdateSourceTrigger=PropertyChanged}" Value="0">
                                                                    <Setter Property="IsEnabled" Value="False"/>
                                                                </DataTrigger>
                                                                <DataTrigger Binding="{Binding Text, ElementName=MinNum_Price, UpdateSourceTrigger=PropertyChanged}" Value="">
                                                                    <Setter Property="IsEnabled" Value="False"/>
                                                                </DataTrigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </RepeatButton.Style>
                                                </RepeatButton>
                                            </Grid>

                                            <TextBlock Grid.Row="2" Text="Maximum" Margin="10" Foreground="Black" />
                                            <Grid Grid.Row="3" VerticalAlignment="Top" HorizontalAlignment="Stretch" Margin="10,0,10,0">
                                                <Grid.RowDefinitions>
                                                    <RowDefinition />
                                                    <RowDefinition />
                                                </Grid.RowDefinitions>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="3*" />
                                                    <ColumnDefinition Width="1*" />
                                                </Grid.ColumnDefinitions>

                                                <TextBox x:Name="MaxNum_Price" Text="" Grid.RowSpan="2" HorizontalAlignment="Stretch" />
                                                <RepeatButton Grid.Column="1" Grid.Row="0" FontSize="8" Content="&#xf0de;" FontFamily="{StaticResource FontAwesome}"  
                                                          VerticalContentAlignment="Center" HorizontalContentAlignment="Center" Click="NUDButtonUP_Click"/>
                                                <RepeatButton Grid.Column="1" Grid.Row="1" FontSize="8"  Content="&#xf0dd;" FontFamily="{StaticResource FontAwesome}" 
                                                          VerticalContentAlignment="Center" HorizontalContentAlignment="Center" Height="13" VerticalAlignment="Bottom" Click="NUDButtonDown_Click">
                                                    <RepeatButton.Style>
                                                        <Style TargetType="{x:Type RepeatButton}">
                                                            <Style.Triggers>
                                                                <DataTrigger Binding="{Binding Text, ElementName=MaxNum_Price, UpdateSourceTrigger=PropertyChanged}" Value="0">
                                                                    <Setter Property="IsEnabled" Value="False"/>
                                                                </DataTrigger>
                                                                <DataTrigger Binding="{Binding Text, ElementName=MaxNum_Price, UpdateSourceTrigger=PropertyChanged}" Value="">
                                                                    <Setter Property="IsEnabled" Value="False"/>
                                                                </DataTrigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </RepeatButton.Style>
                                                </RepeatButton>
                                            </Grid>
                                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Grid.Row="4">
                                                <Button Content="CANCEL" Click="ClosePopUp_Click"  Margin="10" Padding="5" Style="{StaticResource cancelBtn}"/>
                                                <Button Tag="{Binding}" Margin="10" Padding="5" Background="#0094B3" Foreground="White"
                                                    Click="Price_Filter_Apply"  CommandParameter="" Content="APPLY" Style="{StaticResource popUpBtn}"/>
                                            </StackPanel>
                                        </Grid>
                                    </Border>
                                </Popup>
                            </DockPanel>
                        </DataGridTextColumn.Header>
                    </DataGridTextColumn>
                    <DataGridTextColumn  Width="auto" Binding="{Binding Path=Strike}">
                        <DataGridTextColumn.HeaderStyle>
                            <Style TargetType="{x:Type DataGridColumnHeader}">
                                <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                            </Style>
                        </DataGridTextColumn.HeaderStyle>
                        <DataGridTextColumn.Header>
                            <DockPanel>
                                <TextBlock Style="{StaticResource SortDirectionStyle}">
                                    <TextBlock.Triggers></TextBlock.Triggers>
                                </TextBlock>
                                <TextBlock Text="Strike" Margin="10,5" />
                                <ToggleButton Name="Strike"  Click="BtnFilter_Click" 
                                          Style="{Binding Source={StaticResource DropDown_btn}}"/>
                                <Popup x:Name="pop_RangeFlt_Strike" Style="{Binding Source={StaticResource PopUpStyle}}">
                                    <Border Background="White" BorderBrush="Gray" BorderThickness="1">
                                        <Grid>
                                            <Grid.RowDefinitions>
                                                <RowDefinition Height="1*" />
                                                <RowDefinition Height="2*"  />
                                                <RowDefinition Height="1*" />
                                                <RowDefinition Height="2*" />
                                                <RowDefinition Height="1*" />
                                            </Grid.RowDefinitions>

                                            <TextBlock Text="Minimum" Margin="10" Foreground="Black"/>
                                            <Grid Grid.Row="1" VerticalAlignment="Top" HorizontalAlignment="Stretch" Margin="10,0,10,0">
                                                <Grid.RowDefinitions>
                                                    <RowDefinition />
                                                    <RowDefinition />
                                                </Grid.RowDefinitions>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="3*" />
                                                    <ColumnDefinition Width="1*" />
                                                </Grid.ColumnDefinitions>
                                                <TextBox x:Name="MinNum_Strike" Text="" Grid.Row="0" Grid.RowSpan="2" HorizontalAlignment="Stretch"/>
                                                <RepeatButton  Grid.Column="1" Grid.Row="0" FontSize="8" Content="&#xf0de;" FontFamily="{StaticResource FontAwesome}" 
                                      VerticalContentAlignment="Center" HorizontalContentAlignment="Center" Click="NUDButtonUP_Click" />
                                                <RepeatButton  Grid.Column="1" Grid.Row="1" FontSize="8" Content="&#xf0dd;" FontFamily="{StaticResource FontAwesome}" 
                                      VerticalContentAlignment="Center" HorizontalContentAlignment="Center" Height="13" VerticalAlignment="Bottom" Click="NUDButtonDown_Click">
                                                    <RepeatButton.Style>
                                                        <Style TargetType="{x:Type RepeatButton}">
                                                            <Style.Triggers>
                                                                <DataTrigger Binding="{Binding Text, ElementName=MinNum_Strike, UpdateSourceTrigger=PropertyChanged}" Value="0">
                                                                    <Setter Property="IsEnabled" Value="False"/>
                                                                </DataTrigger>
                                                                <DataTrigger Binding="{Binding Text, ElementName=MinNum_Strike, UpdateSourceTrigger=PropertyChanged}" Value="">
                                                                    <Setter Property="IsEnabled" Value="False"/>
                                                                </DataTrigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </RepeatButton.Style>
                                                </RepeatButton>
                                            </Grid>

                                            <TextBlock Grid.Row="2" Text="Maximum" Margin="10" Foreground="Black" />
                                            <Grid Grid.Row="3" VerticalAlignment="Top" HorizontalAlignment="Stretch" Margin="10,0,10,0">
                                                <Grid.RowDefinitions>
                                                    <RowDefinition />
                                                    <RowDefinition />
                                                </Grid.RowDefinitions>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="3*" />
                                                    <ColumnDefinition Width="1*" />
                                                </Grid.ColumnDefinitions>

                                                <TextBox x:Name="MaxNum_Strike" Text="" Grid.RowSpan="2" HorizontalAlignment="Stretch" />
                                                <RepeatButton   Grid.Column="1" Grid.Row="0" FontSize="8" Content="&#xf0de;" FontFamily="{StaticResource FontAwesome}"  
                                                          VerticalContentAlignment="Center" HorizontalContentAlignment="Center" Click="NUDButtonUP_Click"/>
                                                <RepeatButton Grid.Column="1" Grid.Row="1" FontSize="8"  Content="&#xf0dd;" FontFamily="{StaticResource FontAwesome}" 
                                                          VerticalContentAlignment="Center" HorizontalContentAlignment="Center" Height="13" VerticalAlignment="Bottom" Click="NUDButtonDown_Click">
                                                    <RepeatButton.Style>
                                                        <Style TargetType="{x:Type RepeatButton}">
                                                            <Style.Triggers>
                                                                <DataTrigger Binding="{Binding Text, ElementName=MaxNum_Strike, UpdateSourceTrigger=PropertyChanged}" Value="0">
                                                                    <Setter Property="IsEnabled" Value="False"/>
                                                                </DataTrigger>
                                                                <DataTrigger Binding="{Binding Text, ElementName=MaxNum_Strike, UpdateSourceTrigger=PropertyChanged}" Value="">
                                                                    <Setter Property="IsEnabled" Value="False"/>
                                                                </DataTrigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </RepeatButton.Style>
                                                </RepeatButton>
                                            </Grid>
                                            <StackPanel  Orientation="Horizontal" HorizontalAlignment="Center" Grid.Row="4">
                                                <Button Content="CANCEL" Click="ClosePopUp_Click"  Margin="10" Padding="5" Style="{StaticResource cancelBtn}"/>
                                                <Button Tag="{Binding}" Margin="10" Padding="5" Background="#0094B3" Foreground="White"
                                                    Click="Strike_Filter_Apply"  CommandParameter="" Content="APPLY" Style="{StaticResource popUpBtn}"/>
                                            </StackPanel>
                                        </Grid>
                                    </Border>
                                </Popup>
                            </DockPanel>
                        </DataGridTextColumn.Header>
                    </DataGridTextColumn>
                    <DataGridTextColumn  Width="auto" Binding="{Binding Path=QtyInLots}">
                        <DataGridTextColumn.HeaderStyle>
                            <Style TargetType="{x:Type DataGridColumnHeader}">
                                <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                            </Style>
                        </DataGridTextColumn.HeaderStyle>
                        <DataGridTextColumn.Header>
                            <DockPanel>
                                <TextBlock Style="{StaticResource SortDirectionStyle}">
                                    <TextBlock.Triggers></TextBlock.Triggers>
                                </TextBlock>
                                <TextBlock Text="Quantity In Lots" Margin="10,5" />
                                <ToggleButton Name="QtyInLots" Click="BtnFilter_Click" Style="{Binding Source={StaticResource DropDown_btn}}"/>
                                <Popup x:Name="pop_RangeFlt_QtyInLots" Style="{Binding Source={StaticResource PopUpStyle}}">
                                    <Border Background="White" BorderBrush="Gray" BorderThickness="1">
                                        <Grid>
                                            <Grid.RowDefinitions>
                                                <RowDefinition Height="1*" />
                                                <RowDefinition Height="2*"  />
                                                <RowDefinition Height="1*" />
                                                <RowDefinition Height="2*" />
                                                <RowDefinition Height="1*" />
                                            </Grid.RowDefinitions>

                                            <TextBlock Text="Minimum" Margin="10" Foreground="Black"/>
                                            <Grid Grid.Row="1" VerticalAlignment="Top" HorizontalAlignment="Stretch" Margin="10,0,10,0">
                                                <Grid.RowDefinitions>
                                                    <RowDefinition />
                                                    <RowDefinition />
                                                </Grid.RowDefinitions>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="3*" />
                                                    <ColumnDefinition Width="1*" />
                                                </Grid.ColumnDefinitions>
                                                <TextBox x:Name="MinNum_QtyInLots" Text="" Grid.Row="0" Grid.RowSpan="2" HorizontalAlignment="Stretch"/>
                                                <RepeatButton Grid.Column="1" Grid.Row="0" FontSize="8" Content="&#xf0de;" FontFamily="{StaticResource FontAwesome}" 
                                      VerticalContentAlignment="Center" HorizontalContentAlignment="Center" Click="NUDButtonUP_Click" />
                                                <RepeatButton  Grid.Column="1" Grid.Row="1" FontSize="8" Content="&#xf0dd;" FontFamily="{StaticResource FontAwesome}" 
                                      VerticalContentAlignment="Center" HorizontalContentAlignment="Center" Height="13" VerticalAlignment="Bottom" Click="NUDButtonDown_Click">
                                                    <RepeatButton.Style>
                                                        <Style TargetType="{x:Type RepeatButton}">
                                                            <Style.Triggers>
                                                                <DataTrigger Binding="{Binding Text, ElementName=MinNum_QtyInLots, UpdateSourceTrigger=PropertyChanged}" Value="0">
                                                                    <Setter Property="IsEnabled" Value="False"/>
                                                                </DataTrigger>
                                                                <DataTrigger Binding="{Binding Text, ElementName=MinNum_QtyInLots, UpdateSourceTrigger=PropertyChanged}" Value="">
                                                                    <Setter Property="IsEnabled" Value="False"/>
                                                                </DataTrigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </RepeatButton.Style>
                                                </RepeatButton>
                                            </Grid>

                                            <TextBlock Grid.Row="2" Text="Maximum" Margin="10" Foreground="Black" />
                                            <Grid Grid.Row="3" VerticalAlignment="Top" HorizontalAlignment="Stretch" Margin="10,0,10,0">
                                                <Grid.RowDefinitions>
                                                    <RowDefinition />
                                                    <RowDefinition />
                                                </Grid.RowDefinitions>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="3*" />
                                                    <ColumnDefinition Width="1*" />
                                                </Grid.ColumnDefinitions>
                                                <TextBox x:Name="MaxNum_QtyInLots" Text="" Grid.RowSpan="2" HorizontalAlignment="Stretch" />
                                                <RepeatButton Grid.Column="1" Grid.Row="0" FontSize="8" Content="&#xf0de;" FontFamily="{StaticResource FontAwesome}"  
                                                          VerticalContentAlignment="Center" HorizontalContentAlignment="Center" Click="NUDButtonUP_Click"/>
                                                <RepeatButton  Grid.Column="1" Grid.Row="1" FontSize="8"  Content="&#xf0dd;" FontFamily="{StaticResource FontAwesome}" 
                                                          VerticalContentAlignment="Center" HorizontalContentAlignment="Center" Height="13" VerticalAlignment="Bottom" Click="NUDButtonDown_Click">
                                                    <RepeatButton.Style>
                                                        <Style TargetType="{x:Type RepeatButton}">
                                                            <Style.Triggers>
                                                                <DataTrigger Binding="{Binding Text, ElementName=MaxNum_QtyInLots, UpdateSourceTrigger=PropertyChanged}" Value="0">
                                                                    <Setter Property="IsEnabled" Value="False"/>
                                                                </DataTrigger>
                                                                <DataTrigger Binding="{Binding Text, ElementName=MaxNum_QtyInLots, UpdateSourceTrigger=PropertyChanged}" Value="">
                                                                    <Setter Property="IsEnabled" Value="False"/>
                                                                </DataTrigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </RepeatButton.Style>
                                                </RepeatButton>
                                            </Grid>
                                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Grid.Row="4">
                                                <Button Content="CANCEL" Click="ClosePopUp_Click"  Margin="10" Padding="5" Style="{StaticResource cancelBtn}"/>
                                                <Button Tag="{Binding}" Margin="10" Padding="5" Background="#0094B3" Foreground="White"
                                                    Click="QtyInLots_Filter_Apply"  CommandParameter="" Content="APPLY" Style="{StaticResource popUpBtn}"/>
                                            </StackPanel>
                                        </Grid>
                                    </Border>
                                </Popup>
                            </DockPanel>
                        </DataGridTextColumn.Header>
                    </DataGridTextColumn>
                    <DataGridTextColumn  Width="auto" Binding="{Binding Path=TrdStatus}">
                        <DataGridTextColumn.HeaderStyle>
                            <Style TargetType="{x:Type DataGridColumnHeader}">
                                <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                            </Style>
                        </DataGridTextColumn.HeaderStyle>
                        <DataGridTextColumn.Header>
                            <DockPanel>
                                <TextBlock Style="{StaticResource SortDirectionStyle}">
                                    <TextBlock.Triggers>
                                    </TextBlock.Triggers>
                                </TextBlock>
                                <TextBlock Text="Trade Status" Margin="10,5" />
                                <ToggleButton Name="TrdStatus" Click="BtnFilter_Click" Style="{Binding Source={StaticResource DropDown_btn}}" />
                                <Popup Name="pop_CheckboxFlt_TrdStatus" Closed="ClosePopUp" Style="{Binding Source={StaticResource PopUpStyle}}">
                                    <Border Background="White" BorderBrush="Gray" BorderThickness="1">
                                        <Grid>
                                            <Grid.RowDefinitions>
                                                <RowDefinition />
                                                <RowDefinition />
                                                <RowDefinition />
                                                <RowDefinition />
                                            </Grid.RowDefinitions>
                                            <DockPanel Grid.Row="0" Margin="10">
                                                <!--<TextBlock Text="SEARCH" Margin="10" DockPanel.Dock="Left" VerticalAlignment="Center" FontWeight="Bold"/>
                                                <TextBox x:Name="tbFilter_TrdStatus" Margin="10" Tag="TrdStatus" Width="Auto"/>-->
                                            </DockPanel>
                                            <StackPanel Orientation="Vertical" Grid.Row="1" Grid.RowSpan="2">
                                                <CheckBox Grid.Row="1" Name="Checkbox_selectAll_TrdStatus" Margin="10,0,0,0" Tag="{Binding}"  IsChecked="True" Checked="SelectAllCheckBox_Checked" Unchecked="SelectAllCheckBox_Checked">(Select All)</CheckBox>
                                                <ListBox Grid.Row="2" Margin="10" ItemsSource="{Binding}" Style="{Binding Source={StaticResource FilterListbox_Style}}" />
                                            </StackPanel>
                                            <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Center">
                                                <Button Margin="10" Padding="5" Content="CANCEL" Click="ClosePopUp_Click" Style="{StaticResource cancelBtn}"/>
                                                <Button Padding="5" Margin="10" Background="#0094B3" Foreground="White" Content="APPLY" Style="{StaticResource popUpBtn}"
                                                    Click="TrdStatus_Filter_Apply"/>
                                            </StackPanel>
                                        </Grid>
                                    </Border>
                                </Popup>
                            </DockPanel>
                        </DataGridTextColumn.Header>
                    </DataGridTextColumn>
                    <DataGridTextColumn  Width="auto" Binding="{Binding Path=ClearedDate,StringFormat=yyyy-MMM-dd}">
                        <DataGridTextColumn.HeaderStyle>
                            <Style TargetType="{x:Type DataGridColumnHeader}">
                                <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                            </Style>
                        </DataGridTextColumn.HeaderStyle>
                        <DataGridTextColumn.Header>
                            <DockPanel>
                                <TextBlock Style="{StaticResource SortDirectionStyle}">
                                    <TextBlock.Triggers></TextBlock.Triggers>
                                </TextBlock>
                                <TextBlock Text="Cleared date (SGT)" Margin="10,5" />
                                <ToggleButton Name="ClearedDate" Click="BtnFilter_Click" Style="{Binding Source={StaticResource DropDown_btn}}"/>
                                <Popup Name="pop_DateTimeFlt_ClearedDate" Style="{Binding Source={StaticResource PopUpStyle}}">
                                    <Border Background="White" BorderBrush="Gray" BorderThickness="1">
                                        <Grid>
                                            <Grid.RowDefinitions>
                                                <RowDefinition Height="auto" />
                                                <RowDefinition Height="auto" />
                                                <RowDefinition Height="auto" />
                                                <RowDefinition Height="auto" />
                                                <RowDefinition Height="auto" />
                                            </Grid.RowDefinitions>
                                            <TextBlock  Grid.Row="0" Margin="10" Text="FROM"  VerticalAlignment="Center" FontWeight="Bold"/>
                                            <DatePicker Grid.Row="1" Margin="10,0,10,0" Name="datePicker_ClearedDateFrom" SelectedDateFormat="Long" CalendarStyle="{StaticResource styleCalendar}"/>

                                            <TextBlock  Grid.Row="2" Margin="10" Text="TO" VerticalAlignment="Center" FontWeight="Bold"/>
                                            <DatePicker Grid.Row="3" Margin="10,0,10,5" Name="datePicker_ClearedDateTo" SelectedDateFormat="Long" CalendarStyle="{StaticResource styleCalendar}"/>

                                            <StackPanel Grid.Row="4"  Orientation="Horizontal" HorizontalAlignment="Center">
                                                <Button Margin="10" Padding="5" Content="CANCEL" Click="ClosePopUp_Click" Style="{StaticResource cancelBtn}"/>
                                                <Button Margin="10" Padding="5" Background="#0094B3" Foreground="White" Content="APPLY" Click="ClearedDate_Filter_Apply"
                                                        Style="{StaticResource popUpBtn}"/>
                                            </StackPanel>
                                        </Grid>
                                    </Border>
                                </Popup>
                            </DockPanel>
                        </DataGridTextColumn.Header>
                    </DataGridTextColumn>
                    <DataGridTextColumn Width="auto" Binding="{Binding Path=ClearMsg}">
                        <DataGridTextColumn.HeaderStyle>
                            <Style TargetType="{x:Type DataGridColumnHeader}">
                                <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                            </Style>
                        </DataGridTextColumn.HeaderStyle>
                        <DataGridTextColumn.Header>
                            <DockPanel>
                                <TextBlock Style="{StaticResource SortDirectionStyle}">
                                    <TextBlock.Triggers></TextBlock.Triggers>
                                </TextBlock>
                                <TextBlock Text="Clearing Message" Margin="10,5" />
                                <ToggleButton  Name="ClearMsg" Style="{StaticResource DropDown_btn}" Click="BtnFilter_Click"/>
                                <Popup Name="pop_ComboFlt_ClearMsg" Style="{Binding Source={StaticResource PopUpStyle}}">
                                    <Border Background="White" BorderBrush="Gray" BorderThickness="1">
                                        <Grid>
                                            <Grid.RowDefinitions>
                                                <RowDefinition />
                                                <RowDefinition />
                                                <RowDefinition />
                                                <RowDefinition />
                                                <RowDefinition />
                                            </Grid.RowDefinitions>
                                            <TextBlock Text="VALUE" Margin="10"  VerticalAlignment="Center" FontWeight="Bold"/>
                                            <ComboBox Name="TextFltOptions_ClearMsg" Grid.Row="1" Margin="10,0,10,0" SelectedIndex="0" ItemsSource="{StaticResource ComboBoxOptions}"/>
                                            <TextBlock Grid.Row="2" Text="TEXT" Margin="10" DockPanel.Dock="Left" VerticalAlignment="Center" FontWeight="Bold"/>
                                            <TextBox Name="ClearMsg_Flt_Input"  Grid.Row="3" Margin="10,0,10,0"/>

                                            <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Center">
                                                <Button Margin="10" Padding="5" Content="CANCEL" Click="ClosePopUp_Click" Style="{StaticResource cancelBtn}"/>
                                                <Button Padding="5" Margin="10" Background="#0094B3" Foreground="White" Content="APPLY" Style="{StaticResource popUpBtn}"
                                                    Click="ClearMsg_Filter_Apply"/>
                                            </StackPanel>
                                        </Grid>
                                    </Border>
                                </Popup>
                            </DockPanel>
                        </DataGridTextColumn.Header>
                    </DataGridTextColumn>
                    <DataGridTextColumn  Width="auto" Binding="{Binding Path=ApprovalDateTime,StringFormat=dd/MM/yyyy hh:mm tt}">
                        <DataGridTextColumn.HeaderStyle>
                            <Style TargetType="{x:Type DataGridColumnHeader}">
                                <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                            </Style>
                        </DataGridTextColumn.HeaderStyle>
                        <DataGridTextColumn.Header>
                            <DockPanel>
                                <TextBlock Style="{StaticResource SortDirectionStyle}">
                                    <TextBlock.Triggers></TextBlock.Triggers>
                                </TextBlock>
                                <TextBlock Text="Approval Date &amp; Time (SGT)" Margin="10,5" />
                                <ToggleButton Name="ApprovalDateTime" Click="BtnFilter_Click" Style="{Binding Source={StaticResource DropDown_btn}}"/>
                                <Popup Name="pop_DateTimeFlt_ApprovalDateTime" Style="{Binding Source={StaticResource PopUpStyle}}">
                                    <Border Background="White" BorderBrush="Gray" BorderThickness="1">
                                        <Grid>
                                            <Grid.RowDefinitions>
                                                <RowDefinition Height="auto" />
                                                <RowDefinition Height="auto" />
                                                <RowDefinition Height="auto" />
                                                <RowDefinition Height="auto" />
                                                <RowDefinition Height="auto" />
                                            </Grid.RowDefinitions>
                                            <TextBlock  Grid.Row="0" Margin="10" Text="FROM"  VerticalAlignment="Center" FontWeight="Bold"/>
                                            <DatePicker Grid.Row="1" Margin="10,0,10,0" Name="datePicker_ApprovalFrom" SelectedDateFormat="Long" CalendarStyle="{StaticResource styleCalendar}"/>

                                            <TextBlock  Grid.Row="2" Margin="10" Text="TO" VerticalAlignment="Center" FontWeight="Bold"/>
                                            <DatePicker Grid.Row="3" Margin="10,0,10,5" Name="datePicker_ApprovalTTo" SelectedDateFormat="Long" CalendarStyle="{StaticResource styleCalendar}"/>

                                            <StackPanel Grid.Row="4"  Orientation="Horizontal" HorizontalAlignment="Center">
                                                <Button Margin="10" Padding="5" Content="CANCEL" Click="ClosePopUp_Click" Style="{StaticResource cancelBtn}"/>
                                                <Button Margin="10" Padding="5" Background="#0094B3" Foreground="White" Content="APPLY" 
                                                        Style="{StaticResource popUpBtn}" Click="ApprovalDT_Filter_Apply"/>
                                            </StackPanel>
                                        </Grid>
                                    </Border>
                                </Popup>
                            </DockPanel>
                        </DataGridTextColumn.Header>
                    </DataGridTextColumn>
                    <DataGridTextColumn  Width="auto" Binding="{Binding Path=TrdExecDateTime,StringFormat=dd/MM/yyyy hh:mm tt}">
                        <DataGridTextColumn.HeaderStyle>
                            <Style TargetType="{x:Type DataGridColumnHeader}">
                                <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                            </Style>
                        </DataGridTextColumn.HeaderStyle>
                        <DataGridTextColumn.Header>
                            <DockPanel>
                                <TextBlock Style="{StaticResource SortDirectionStyle}">
                                    <TextBlock.Triggers></TextBlock.Triggers>
                                </TextBlock>
                                <TextBlock Text="Trade Execution Date &amp; Time (SGT)" Margin="10,5" />
                                <ToggleButton Name="TrdExecDateTime" Click="BtnFilter_Click" Style="{Binding Source={StaticResource DropDown_btn}}"/>
                                <Popup Name="pop_DateTimeFlt_TrdExecDateTime" Style="{Binding Source={StaticResource PopUpStyle}}">
                                    <Border Background="White" BorderBrush="Gray" BorderThickness="1">
                                        <Grid>
                                            <Grid.RowDefinitions>
                                                <RowDefinition Height="auto" />
                                                <RowDefinition Height="auto" />
                                                <RowDefinition Height="auto" />
                                                <RowDefinition Height="auto" />
                                                <RowDefinition Height="auto" />
                                            </Grid.RowDefinitions>
                                            <TextBlock  Grid.Row="0" Margin="10" Text="FROM"  VerticalAlignment="Center" FontWeight="Bold"/>
                                            <DatePicker Grid.Row="1" Margin="10,0,10,0" Name="datePicker_TrdExecFrom" 
                                                    SelectedDateFormat="Long" CalendarStyle="{StaticResource styleCalendar}"/>

                                            <TextBlock  Grid.Row="2" Margin="10" Text="TO" VerticalAlignment="Center" FontWeight="Bold"/>
                                            <DatePicker Grid.Row="3" Margin="10,0,10,5" Name="datePicker_TrdExecTo"
                                                    SelectedDateFormat="Long" CalendarStyle="{StaticResource styleCalendar}"/>

                                            <StackPanel Grid.Row="4"  Orientation="Horizontal" HorizontalAlignment="Center">
                                                <Button Margin="10" Padding="5" Content="CANCEL" Click="ClosePopUp_Click" Style="{StaticResource cancelBtn}" />
                                                <Button Margin="10" Padding="5" Background="#0094B3" Foreground="White" Content="APPLY" 
                                                        Style="{StaticResource popUpBtn}" Click="TrdExecDT_Filter_Apply"/>
                                            </StackPanel>
                                        </Grid>
                                    </Border>
                                </Popup>
                            </DockPanel>
                        </DataGridTextColumn.Header>
                    </DataGridTextColumn>
                    <DataGridTextColumn  Width="auto" Binding="{Binding Path=BuyerCM}">
                        <DataGridTextColumn.HeaderStyle>
                            <Style TargetType="{x:Type DataGridColumnHeader}">
                                <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                            </Style>
                        </DataGridTextColumn.HeaderStyle>
                        <DataGridTextColumn.Header>
                            <DockPanel>
                                <TextBlock Style="{StaticResource SortDirectionStyle}">
                                    <TextBlock.Triggers></TextBlock.Triggers>
                                </TextBlock>
                                <TextBlock Text="Buyer CM" Margin="10,5" />
                                <ToggleButton Name="BuyerCM" Click="BtnFilter_Click" Style="{Binding Source={StaticResource DropDown_btn}}" />
                                <Popup Name="pop_CheckboxFlt_BuyerCM" Closed="ClosePopUp" Style="{Binding Source={StaticResource PopUpStyle}}">
                                    <Border Background="White" BorderBrush="Gray" BorderThickness="1">
                                        <Grid>
                                            <Grid.RowDefinitions>
                                                <RowDefinition />
                                                <RowDefinition />
                                                <RowDefinition />
                                                <RowDefinition />
                                            </Grid.RowDefinitions>
                                            <DockPanel Grid.Row="0" Margin="10">
                                                <!--<TextBlock Text="SEARCH" Margin="10" DockPanel.Dock="Left" VerticalAlignment="Center" FontWeight="Bold"/>
                                                <TextBox x:Name="tbFilter_BuyerCM" Margin="10" Tag="BuyerCM" Width="Auto"/>-->
                                            </DockPanel>
                                            <StackPanel Orientation="Vertical" Grid.Row="1" Grid.RowSpan="2">
                                                <CheckBox Grid.Row="1" Name="Checkbox_selectAll_BuyerCM" Margin="10,0,0,0" Tag="{Binding}"  IsChecked="True" Checked="SelectAllCheckBox_Checked" Unchecked="SelectAllCheckBox_Checked">(Select All)</CheckBox>
                                                <ListBox Grid.Row="2" Margin="10" ItemsSource="{Binding}" Style="{Binding Source={StaticResource FilterListbox_Style}}" />
                                            </StackPanel>
                                            <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Center">
                                                <Button Margin="10" Padding="5" Content="CANCEL" Click="ClosePopUp_Click" Style="{StaticResource cancelBtn}"/>
                                                <Button Padding="5" Margin="10" Background="#0094B3" Foreground="White" Content="APPLY" Style="{StaticResource popUpBtn}"
                                                    Click="BuyerCM_Filter_Apply"/>
                                            </StackPanel>
                                        </Grid>
                                    </Border>
                                </Popup>
                            </DockPanel>
                        </DataGridTextColumn.Header>
                    </DataGridTextColumn>
                    <DataGridTextColumn  Width="auto" Binding="{Binding Path=BuyerTrdCom}">
                        <DataGridTextColumn.HeaderStyle>
                            <Style TargetType="{x:Type DataGridColumnHeader}">
                                <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                            </Style>
                        </DataGridTextColumn.HeaderStyle>
                        <DataGridTextColumn.Header>
                            <DockPanel>
                                <TextBlock Style="{StaticResource SortDirectionStyle}">
                                    <TextBlock.Triggers></TextBlock.Triggers>
                                </TextBlock>
                                <TextBlock Text="Buyer Trading Company" Margin="10,5" />
                                <ToggleButton Name="BuyerTrdCom" Click="BtnFilter_Click" Style="{Binding Source={StaticResource DropDown_btn}}" />
                                <Popup Name="pop_CheckboxFlt_BuyerTrdCom" Closed="ClosePopUp" Style="{Binding Source={StaticResource PopUpStyle}}">
                                    <Border Background="White" BorderBrush="Gray" BorderThickness="1">
                                        <Grid>
                                            <Grid.RowDefinitions>
                                                <RowDefinition />
                                                <RowDefinition />
                                                <RowDefinition />
                                                <RowDefinition />
                                            </Grid.RowDefinitions>
                                            <DockPanel Grid.Row="0" Margin="10">
                                                <!--<TextBlock Text="SEARCH" Margin="10" DockPanel.Dock="Left" VerticalAlignment="Center" FontWeight="Bold"/>
                                                <TextBox x:Name="tbFilter_BuyerTrdCom" Margin="10" Tag="BuyerTrdCom" Width="Auto"/>-->
                                            </DockPanel>
                                            <StackPanel Orientation="Vertical" Grid.Row="1" Grid.RowSpan="2">
                                                <CheckBox Grid.Row="1" Name="Checkbox_selectAll_BuyerTrdCom" Margin="10,0,0,0" Tag="{Binding}"  IsChecked="True" Checked="SelectAllCheckBox_Checked" Unchecked="SelectAllCheckBox_Checked">(Select All)</CheckBox>
                                                <ListBox Grid.Row="2" Margin="10" ItemsSource="{Binding}" Style="{Binding Source={StaticResource FilterListbox_Style}}" />
                                            </StackPanel>
                                            <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Center">
                                                <Button Margin="10" Padding="5" Content="CANCEL" Click="ClosePopUp_Click" Style="{StaticResource cancelBtn}"/>
                                                <Button Padding="5" Margin="10" Background="#0094B3" Foreground="White" Content="APPLY" Style="{StaticResource popUpBtn}"
                                                    Click="BuyerTrdCom_Filter_Apply"/>
                                            </StackPanel>
                                        </Grid>
                                    </Border>
                                </Popup>
                            </DockPanel>
                        </DataGridTextColumn.Header>
                    </DataGridTextColumn>
                    <DataGridTextColumn Width="auto" Binding="{Binding Path=BuyerTrader}">
                        <DataGridTextColumn.HeaderStyle>
                            <Style TargetType="{x:Type DataGridColumnHeader}">
                                <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                            </Style>
                        </DataGridTextColumn.HeaderStyle>
                        <DataGridTextColumn.Header>
                            <DockPanel>
                                <TextBlock Style="{StaticResource SortDirectionStyle}">
                                    <TextBlock.Triggers></TextBlock.Triggers>
                                </TextBlock>
                                <TextBlock Text="Buyer Trader" Margin="10,5" />
                                <ToggleButton  Name="BuyerTrader" Style="{StaticResource DropDown_btn}" Click="BtnFilter_Click"/>
                                <Popup Name="pop_ComboFlt_BuyerTrader" Style="{Binding Source={StaticResource PopUpStyle}}">
                                    <Border Background="White" BorderBrush="Gray" BorderThickness="1">
                                        <Grid>
                                            <Grid.RowDefinitions>
                                                <RowDefinition />
                                                <RowDefinition />
                                                <RowDefinition />
                                                <RowDefinition />
                                                <RowDefinition />
                                            </Grid.RowDefinitions>
                                            <TextBlock Text="VALUE" Margin="10"  VerticalAlignment="Center" FontWeight="Bold"/>
                                            <ComboBox Name="TextFltOptions_BuyerTrader" Grid.Row="1" Margin="10,0,10,0" SelectedIndex="0" ItemsSource="{StaticResource ComboBoxOptions}"/>
                                            <TextBlock Grid.Row="2" Text="TEXT" Margin="10" DockPanel.Dock="Left" VerticalAlignment="Center" FontWeight="Bold"/>
                                            <TextBox Name="BuyerTrader_Flt_Input"  Grid.Row="3" Margin="10,0,10,0"/>

                                            <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Center">
                                                <Button Margin="10" Padding="5" Content="CANCEL" Click="ClosePopUp_Click" Style="{StaticResource cancelBtn}"/>
                                                <Button Padding="5" Margin="10" Background="#0094B3" Foreground="White" Content="APPLY" 
                                                        Style="{StaticResource popUpBtn}" Click="BuyerTrader_Filter_Apply"/>
                                            </StackPanel>
                                        </Grid>
                                    </Border>
                                </Popup>
                            </DockPanel>
                        </DataGridTextColumn.Header>
                    </DataGridTextColumn>
                    <DataGridTextColumn  Width="auto" Binding="{Binding Path=BuyerTradeRegistrant}">
                        <DataGridTextColumn.HeaderStyle>
                            <Style TargetType="{x:Type DataGridColumnHeader}">
                                <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                            </Style>
                        </DataGridTextColumn.HeaderStyle>
                        <DataGridTextColumn.Header>
                            <DockPanel>
                                <TextBlock Style="{StaticResource SortDirectionStyle}">
                                    <TextBlock.Triggers></TextBlock.Triggers>
                                </TextBlock>
                                <TextBlock Text="Buyer Trade Registrant" Margin="10,5" />
                                <ToggleButton Name="BuyerTradeRegistrant" Click="BtnFilter_Click" Style="{Binding Source={StaticResource DropDown_btn}}" />
                                <Popup Name="pop_CheckboxFlt_BuyerTradeRegistrant" Closed="ClosePopUp" Style="{Binding Source={StaticResource PopUpStyle}}">
                                    <Border Background="White" BorderBrush="Gray" BorderThickness="1">
                                        <Grid>
                                            <Grid.RowDefinitions>
                                                <RowDefinition />
                                                <RowDefinition />
                                                <RowDefinition />
                                                <RowDefinition />
                                            </Grid.RowDefinitions>
                                            <DockPanel Grid.Row="0" Margin="10">
                                                <!--<TextBlock Text="SEARCH" Margin="10" DockPanel.Dock="Left" VerticalAlignment="Center" FontWeight="Bold"/>
                                                <TextBox x:Name="tbFilter_BuyerTradeRegistrant" Margin="10" Tag="BuyerTradeRegistrant" Width="Auto"/>-->
                                            </DockPanel>
                                            <StackPanel Orientation="Vertical" Grid.Row="1" Grid.RowSpan="2">
                                                <CheckBox Grid.Row="1" Name="Checkbox_selectAll_BuyerTradeRegistrant" Margin="10,0,0,0" Tag="{Binding}"  IsChecked="True" Checked="SelectAllCheckBox_Checked" Unchecked="SelectAllCheckBox_Checked">(Select All)</CheckBox>
                                                <ListBox Grid.Row="2" Margin="10" ItemsSource="{Binding}" Style="{Binding Source={StaticResource FilterListbox_Style}}" />
                                            </StackPanel>
                                            <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Center">
                                                <Button Margin="10" Padding="5" Content="CANCEL" Click="ClosePopUp_Click" Style="{StaticResource cancelBtn}"/>
                                                <Button Padding="5" Margin="10" Background="#0094B3" Foreground="White" Content="APPLY" Style="{StaticResource popUpBtn}"
                                                    Click="BuyerTradeRegistrant_Filter_Apply"/>
                                            </StackPanel>
                                        </Grid>
                                    </Border>
                                </Popup>
                            </DockPanel>
                        </DataGridTextColumn.Header>
                    </DataGridTextColumn>
                    <DataGridTextColumn Width="auto" Binding="{Binding Path=BuyerAccount}">
                        <DataGridTextColumn.HeaderStyle>
                            <Style TargetType="{x:Type DataGridColumnHeader}">
                                <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                            </Style>
                        </DataGridTextColumn.HeaderStyle>
                        <DataGridTextColumn.Header>
                            <DockPanel>
                                <TextBlock Style="{StaticResource SortDirectionStyle}">
                                    <TextBlock.Triggers></TextBlock.Triggers>
                                </TextBlock>
                                <TextBlock Text="Buyer Account" Margin="10,5" />
                                <ToggleButton  Name="BuyerAccount" Style="{StaticResource DropDown_btn}" Click="BtnFilter_Click"/>
                                <Popup Name="pop_ComboFlt_BuyerAccount" Style="{Binding Source={StaticResource PopUpStyle}}">
                                    <Border Background="White" BorderBrush="Gray" BorderThickness="1">
                                        <Grid>
                                            <Grid.RowDefinitions>
                                                <RowDefinition />
                                                <RowDefinition />
                                                <RowDefinition />
                                                <RowDefinition />
                                                <RowDefinition />
                                            </Grid.RowDefinitions>
                                            <TextBlock Text="VALUE" Margin="10"  VerticalAlignment="Center" FontWeight="Bold"/>
                                            <ComboBox Name="TextFltOptions_BuyerAccount" Grid.Row="1" Margin="10,0,10,0" SelectedIndex="0" ItemsSource="{StaticResource ComboBoxOptions}"/>
                                            <TextBlock Grid.Row="2" Text="TEXT" Margin="10" DockPanel.Dock="Left" VerticalAlignment="Center" FontWeight="Bold"/>
                                            <TextBox Name="BuyerAccount_Flt_Input"  Grid.Row="3" Margin="10,0,10,0"/>

                                            <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Center">
                                                <Button Margin="10" Padding="5" Content="CANCEL" Click="ClosePopUp_Click" Style="{StaticResource cancelBtn}"/>
                                                <Button Padding="5" Margin="10" Background="#0094B3" Foreground="White" Content="APPLY" Style="{StaticResource popUpBtn}" Click="BuyerAccount_Filter_Apply"/>
                                            </StackPanel>
                                        </Grid>
                                    </Border>
                                </Popup>
                            </DockPanel>
                        </DataGridTextColumn.Header>
                    </DataGridTextColumn>
                    <DataGridTextColumn Width="auto" Binding="{Binding Path=BuyerSecurID}">
                        <DataGridTextColumn.HeaderStyle>
                            <Style TargetType="{x:Type DataGridColumnHeader}">
                                <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                            </Style>
                        </DataGridTextColumn.HeaderStyle>
                        <DataGridTextColumn.Header>
                            <DockPanel>
                                <TextBlock Style="{StaticResource SortDirectionStyle}">
                                    <TextBlock.Triggers></TextBlock.Triggers>
                                </TextBlock>
                                <TextBlock Text="Buyer Secur ID" Margin="10,5" />
                                <ToggleButton  Name="BuyerSecurID" Style="{StaticResource DropDown_btn}" Click="BtnFilter_Click"/>
                                <Popup Name="pop_ComboFlt_BuyerSecurID" Style="{Binding Source={StaticResource PopUpStyle}}">
                                    <Border Background="White" BorderBrush="Gray" BorderThickness="1">
                                        <Grid>
                                            <Grid.RowDefinitions>
                                                <RowDefinition />
                                                <RowDefinition />
                                                <RowDefinition />
                                                <RowDefinition />
                                                <RowDefinition />
                                            </Grid.RowDefinitions>
                                            <TextBlock Text="VALUE" Margin="10"  VerticalAlignment="Center" FontWeight="Bold"/>
                                            <ComboBox Name="TextFltOptions_BuyerSecurID" Grid.Row="1" Margin="10,0,10,0" SelectedIndex="0" ItemsSource="{StaticResource ComboBoxOptions}"/>
                                            <TextBlock Grid.Row="2" Text="TEXT" Margin="10" DockPanel.Dock="Left" VerticalAlignment="Center" FontWeight="Bold"/>
                                            <TextBox Name="BuyerSecurID_Flt_Input"  Grid.Row="3" Margin="10,0,10,0"/>

                                            <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Center">
                                                <Button Margin="10" Padding="5" Content="CANCEL" Click="ClosePopUp_Click" Style="{StaticResource cancelBtn}"/>
                                                <Button Padding="5" Margin="10" Background="#0094B3" Foreground="White" Content="APPLY" Style="{StaticResource popUpBtn}" Click="BuyerSecurID_Filter_Apply"/>
                                            </StackPanel>
                                        </Grid>
                                    </Border>
                                </Popup>
                            </DockPanel>
                        </DataGridTextColumn.Header>
                    </DataGridTextColumn>
                    <DataGridTextColumn  Width="auto" Binding="{Binding Path=BuyerBroker}">
                        <DataGridTextColumn.HeaderStyle>
                            <Style TargetType="{x:Type DataGridColumnHeader}">
                                <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                            </Style>
                        </DataGridTextColumn.HeaderStyle>
                        <DataGridTextColumn.Header>
                            <DockPanel>
                                <TextBlock Style="{StaticResource SortDirectionStyle}">
                                    <TextBlock.Triggers></TextBlock.Triggers>
                                </TextBlock>
                                <TextBlock Text="Buyer Broker" Margin="10,5" />
                                <ToggleButton Name="BuyerBroker" Click="BtnFilter_Click" Style="{Binding Source={StaticResource DropDown_btn}}" />
                                <Popup Name="pop_CheckboxFlt_BuyerBroker" Closed="ClosePopUp" Style="{Binding Source={StaticResource PopUpStyle}}">
                                    <Border Background="White" BorderBrush="Gray" BorderThickness="1">
                                        <Grid>
                                            <Grid.RowDefinitions>
                                                <RowDefinition />
                                                <RowDefinition />
                                                <RowDefinition />
                                                <RowDefinition />
                                            </Grid.RowDefinitions>
                                            <DockPanel Grid.Row="0" Margin="10">
                                                <!--<TextBlock Text="SEARCH" Margin="10" DockPanel.Dock="Left" VerticalAlignment="Center" FontWeight="Bold"/>
                                                <TextBox x:Name="tbFilter_BuyerBroker" Margin="10" Tag="BuyerBroker" Width="Auto"/>-->
                                            </DockPanel>
                                            <StackPanel Orientation="Vertical" Grid.Row="1" Grid.RowSpan="2">
                                                <CheckBox Grid.Row="1" Name="Checkbox_selectAll_BuyerBroker" Margin="10,0,0,0" Tag="{Binding}"  IsChecked="True" Checked="SelectAllCheckBox_Checked" Unchecked="SelectAllCheckBox_Checked">(Select All)</CheckBox>
                                                <ListBox Grid.Row="2" Margin="10" ItemsSource="{Binding}" Style="{Binding Source={StaticResource FilterListbox_Style}}" />
                                            </StackPanel>
                                            <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Center">
                                                <Button Margin="10" Padding="5" Content="CANCEL" Click="ClosePopUp_Click" Style="{StaticResource cancelBtn}" />
                                                <Button Padding="5" Margin="10" Background="#0094B3" Foreground="White" Content="APPLY" Style="{StaticResource popUpBtn}"
                                                    Click="BuyerBroker_Filter_Apply"/>
                                            </StackPanel>
                                        </Grid>
                                    </Border>
                                </Popup>
                            </DockPanel>
                        </DataGridTextColumn.Header>
                    </DataGridTextColumn>
                    <DataGridTextColumn Width="auto" Binding="{Binding Path=BuyerComment}">
                        <DataGridTextColumn.HeaderStyle>
                            <Style TargetType="{x:Type DataGridColumnHeader}">
                                <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                            </Style>
                        </DataGridTextColumn.HeaderStyle>
                        <DataGridTextColumn.Header>
                            <DockPanel>
                                <TextBlock Style="{StaticResource SortDirectionStyle}">
                                    <TextBlock.Triggers></TextBlock.Triggers>
                                </TextBlock>
                                <TextBlock Text="Buyer Comment" Margin="10,5" />
                                <ToggleButton  Name="BuyerComment" Style="{StaticResource DropDown_btn}" Click="BtnFilter_Click"/>
                                <Popup Name="pop_ComboFlt_BuyerComment" Style="{Binding Source={StaticResource PopUpStyle}}">
                                    <Border Background="White" BorderBrush="Gray" BorderThickness="1">
                                        <Grid>
                                            <Grid.RowDefinitions>
                                                <RowDefinition />
                                                <RowDefinition />
                                                <RowDefinition />
                                                <RowDefinition />
                                                <RowDefinition />
                                            </Grid.RowDefinitions>
                                            <TextBlock Text="VALUE" Margin="10"  VerticalAlignment="Center" FontWeight="Bold"/>
                                            <ComboBox Name="TextFltOptions_BuyerComment" Grid.Row="1" Margin="10,0,10,0" SelectedIndex="0" ItemsSource="{StaticResource ComboBoxOptions}"/>
                                            <TextBlock Grid.Row="2" Text="TEXT" Margin="10" DockPanel.Dock="Left" VerticalAlignment="Center" FontWeight="Bold"/>
                                            <TextBox Name="BuyerComment_Flt_Input"  Grid.Row="3" Margin="10,0,10,0"/>

                                            <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Center">
                                                <Button Margin="10" Padding="5" Content="CANCEL" Click="ClosePopUp_Click" Style="{StaticResource cancelBtn}"/>
                                                <Button Padding="5" Margin="10" Background="#0094B3" Foreground="White" Content="APPLY" Click="BuyerComment_Filter_Apply" Style="{StaticResource popUpBtn}"/>
                                            </StackPanel>
                                        </Grid>
                                    </Border>
                                </Popup>
                            </DockPanel>
                        </DataGridTextColumn.Header>
                    </DataGridTextColumn>
                    <DataGridTextColumn Width="auto" Binding="{Binding Path=BuyerInternalTrdID}">
                        <DataGridTextColumn.HeaderStyle>
                            <Style TargetType="{x:Type DataGridColumnHeader}">
                                <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                            </Style>
                        </DataGridTextColumn.HeaderStyle>
                        <DataGridTextColumn.Header>
                            <DockPanel>
                                <TextBlock Style="{StaticResource SortDirectionStyle}">
                                    <TextBlock.Triggers></TextBlock.Triggers>
                                </TextBlock>
                                <TextBlock Text="Buyer Internal Trade ID" Margin="10,5" />
                                <ToggleButton  Name="BuyerInternalTrdID" Style="{StaticResource DropDown_btn}" Click="BtnFilter_Click"/>
                                <Popup Name="pop_ComboFlt_BuyerInternalTrdID" Style="{Binding Source={StaticResource PopUpStyle}}">
                                    <Border Background="White" BorderBrush="Gray" BorderThickness="1">
                                        <Grid>
                                            <Grid.RowDefinitions>
                                                <RowDefinition />
                                                <RowDefinition />
                                                <RowDefinition />
                                                <RowDefinition />
                                                <RowDefinition />
                                            </Grid.RowDefinitions>
                                            <TextBlock Text="VALUE" Margin="10"  VerticalAlignment="Center" FontWeight="Bold"/>
                                            <ComboBox Name="TextFltOptions_BuyerInternalTrdID" Grid.Row="1" Margin="10,0,10,0" SelectedIndex="0" ItemsSource="{StaticResource ComboBoxOptions}"/>
                                            <TextBlock Grid.Row="2" Text="TEXT" Margin="10" DockPanel.Dock="Left" VerticalAlignment="Center" FontWeight="Bold"/>
                                            <TextBox Name="BuyerInternalTrdID_Flt_Input"  Grid.Row="3" Margin="10,0,10,0"/>

                                            <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Center">
                                                <Button Margin="10" Padding="5" Content="CANCEL" Click="ClosePopUp_Click" Style="{StaticResource cancelBtn}" />
                                                <Button Padding="5" Margin="10" Background="#0094B3" Foreground="White" Content="APPLY" Click="BuyerInternalTrdID_Filter_Apply" Style="{StaticResource popUpBtn}"/>
                                            </StackPanel>
                                        </Grid>
                                    </Border>
                                </Popup>
                            </DockPanel>
                        </DataGridTextColumn.Header>
                    </DataGridTextColumn>
                    <DataGridTextColumn Width="auto" Binding="{Binding Path=BuySource}">
                        <DataGridTextColumn.HeaderStyle>
                            <Style TargetType="{x:Type DataGridColumnHeader}">
                                <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                            </Style>
                        </DataGridTextColumn.HeaderStyle>
                        <DataGridTextColumn.Header>
                            <DockPanel>
                                <TextBlock Style="{StaticResource SortDirectionStyle}">
                                    <TextBlock.Triggers></TextBlock.Triggers>
                                </TextBlock>
                                <TextBlock Text="Buy Source" Margin="10,5" />
                                <ToggleButton  Name="BuySource" Style="{StaticResource DropDown_btn}" Click="BtnFilter_Click"/>
                                <Popup Name="pop_ComboFlt_BuySource" Style="{Binding Source={StaticResource PopUpStyle}}">
                                    <Border Background="White" BorderBrush="Gray" BorderThickness="1">
                                        <Grid>
                                            <Grid.RowDefinitions>
                                                <RowDefinition />
                                                <RowDefinition />
                                                <RowDefinition />
                                                <RowDefinition />
                                                <RowDefinition />
                                            </Grid.RowDefinitions>
                                            <TextBlock Text="VALUE" Margin="10"  VerticalAlignment="Center" FontWeight="Bold"/>
                                            <ComboBox Name="TextFltOptions_BuySource" Grid.Row="1" Margin="10,0,10,0" SelectedIndex="0" ItemsSource="{StaticResource ComboBoxOptions}"/>
                                            <TextBlock Grid.Row="2" Text="TEXT" Margin="10" DockPanel.Dock="Left" VerticalAlignment="Center" FontWeight="Bold"/>
                                            <TextBox Name="BuySource_Flt_Input"  Grid.Row="3" Margin="10,0,10,0"/>

                                            <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Center">
                                                <Button Margin="10" Padding="5" Content="CANCEL" Click="ClosePopUp_Click" Style="{StaticResource cancelBtn}"/>
                                                <Button Padding="5" Margin="10" Background="#0094B3" Foreground="White" Content="APPLY" Click="BuySource_Filter_Apply" Style="{StaticResource popUpBtn}"/>
                                            </StackPanel>
                                        </Grid>
                                    </Border>
                                </Popup>
                            </DockPanel>
                        </DataGridTextColumn.Header>
                    </DataGridTextColumn>
                    <DataGridTextColumn  Width="auto" Binding="{Binding Path=SellerCM}">
                        <DataGridTextColumn.HeaderStyle>
                            <Style TargetType="{x:Type DataGridColumnHeader}">
                                <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                            </Style>
                        </DataGridTextColumn.HeaderStyle>
                        <DataGridTextColumn.Header>
                            <DockPanel>
                                <TextBlock Style="{StaticResource SortDirectionStyle}">
                                    <TextBlock.Triggers></TextBlock.Triggers>
                                </TextBlock>
                                <TextBlock Text="Seller CM" Margin="10,5" />
                                <ToggleButton Name="SellerCM" Click="BtnFilter_Click" Style="{Binding Source={StaticResource DropDown_btn}}" />
                                <Popup Name="pop_CheckboxFlt_SellerCM" Closed="ClosePopUp" Style="{Binding Source={StaticResource PopUpStyle}}">
                                    <Border Background="White" BorderBrush="Gray" BorderThickness="1">
                                        <Grid>
                                            <Grid.RowDefinitions>
                                                <RowDefinition />
                                                <RowDefinition />
                                                <RowDefinition />
                                                <RowDefinition />
                                            </Grid.RowDefinitions>
                                            <DockPanel Grid.Row="0" Margin="10">
                                                <!--<TextBlock Text="SEARCH" Margin="10" DockPanel.Dock="Left" VerticalAlignment="Center" FontWeight="Bold"/>
                                                <TextBox x:Name="tbFilter_SellerCM" Margin="10" Tag="SellerCM" Width="Auto"/>-->
                                            </DockPanel>
                                            <StackPanel Orientation="Vertical" Grid.Row="1" Grid.RowSpan="2">
                                                <CheckBox Grid.Row="1" Name="Checkbox_selectAll_SellerCM" Margin="10,0,0,0" Tag="{Binding}"  IsChecked="True" Checked="SelectAllCheckBox_Checked" Unchecked="SelectAllCheckBox_Checked">(Select All)</CheckBox>
                                                <ListBox Grid.Row="2" Margin="10" ItemsSource="{Binding}" Style="{Binding Source={StaticResource FilterListbox_Style}}" />
                                            </StackPanel>
                                            <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Center">
                                                <Button Margin="10" Padding="5" Content="CANCEL" Click="ClosePopUp_Click" Style="{StaticResource cancelBtn}"/>
                                                <Button Padding="5" Margin="10" Background="#0094B3" Foreground="White" Content="APPLY" Style="{StaticResource popUpBtn}"
                                                    Click="SellerCM_Filter_Apply"/>
                                            </StackPanel>
                                        </Grid>
                                    </Border>
                                </Popup>
                            </DockPanel>
                        </DataGridTextColumn.Header>
                    </DataGridTextColumn>
                    <DataGridTextColumn  Width="auto" Binding="{Binding Path=SellerTrdCom}">
                        <DataGridTextColumn.HeaderStyle>
                            <Style TargetType="{x:Type DataGridColumnHeader}">
                                <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                            </Style>
                        </DataGridTextColumn.HeaderStyle>
                        <DataGridTextColumn.Header>
                            <DockPanel>
                                <TextBlock Style="{StaticResource SortDirectionStyle}">
                                    <TextBlock.Triggers></TextBlock.Triggers>
                                </TextBlock>
                                <TextBlock Text="Seller Trading Company" Margin="10,5" />
                                <ToggleButton Name="SellerTrdCom" Click="BtnFilter_Click" Style="{Binding Source={StaticResource DropDown_btn}}" />
                                <Popup Name="pop_CheckboxFlt_SellerTrdCom" Closed="ClosePopUp" Style="{Binding Source={StaticResource PopUpStyle}}">
                                    <Border Background="White" BorderBrush="Gray" BorderThickness="1">
                                        <Grid>
                                            <Grid.RowDefinitions>
                                                <RowDefinition />
                                                <RowDefinition />
                                                <RowDefinition />
                                                <RowDefinition />
                                            </Grid.RowDefinitions>
                                            <DockPanel Grid.Row="0" Margin="10">
                                                <!--<TextBlock Text="SEARCH" Margin="10" DockPanel.Dock="Left" VerticalAlignment="Center" FontWeight="Bold"/>
                                                <TextBox x:Name="tbFilter_SellerTrdCom" Margin="10" Tag="SellerTrdCom" Width="Auto"/>-->
                                            </DockPanel>
                                            <StackPanel Orientation="Vertical" Grid.Row="1" Grid.RowSpan="2">
                                                <CheckBox Grid.Row="1" Name="Checkbox_selectAll_SellerTrdCom" Margin="10,0,0,0" Tag="{Binding}"  IsChecked="True" Checked="SelectAllCheckBox_Checked" Unchecked="SelectAllCheckBox_Checked">(Select All)</CheckBox>
                                                <ListBox Grid.Row="2" Margin="10" ItemsSource="{Binding}" Style="{Binding Source={StaticResource FilterListbox_Style}}" />
                                            </StackPanel>
                                            <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Center">
                                                <Button Margin="10" Padding="5" Content="CANCEL" Click="ClosePopUp_Click" Style="{StaticResource cancelBtn}"/>
                                                <Button Padding="5" Margin="10" Background="#0094B3" Foreground="White" Content="APPLY" Style="{StaticResource popUpBtn}"
                                                    Click="SellerTrdCom_Filter_Apply"/>
                                            </StackPanel>
                                        </Grid>
                                    </Border>
                                </Popup>
                            </DockPanel>
                        </DataGridTextColumn.Header>
                    </DataGridTextColumn>
                    <DataGridTextColumn Width="auto" Binding="{Binding Path=SellerTrader}">
                        <DataGridTextColumn.HeaderStyle>
                            <Style TargetType="{x:Type DataGridColumnHeader}">
                                <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                            </Style>
                        </DataGridTextColumn.HeaderStyle>
                        <DataGridTextColumn.Header>
                            <DockPanel>
                                <TextBlock Style="{StaticResource SortDirectionStyle}">
                                    <TextBlock.Triggers></TextBlock.Triggers>
                                </TextBlock>
                                <TextBlock Text="Seller Trader" Margin="10,5" />
                                <ToggleButton  Name="SellerTrader" Style="{StaticResource DropDown_btn}" Click="BtnFilter_Click"/>
                                <Popup Name="pop_ComboFlt_SellerTrader" Style="{Binding Source={StaticResource PopUpStyle}}">
                                    <Border Background="White" BorderBrush="Gray" BorderThickness="1">
                                        <Grid>
                                            <Grid.RowDefinitions>
                                                <RowDefinition />
                                                <RowDefinition />
                                                <RowDefinition />
                                                <RowDefinition />
                                                <RowDefinition />
                                            </Grid.RowDefinitions>
                                            <TextBlock Text="VALUE" Margin="10"  VerticalAlignment="Center" FontWeight="Bold"/>
                                            <ComboBox Name="TextFltOptions_SellerTrader" Grid.Row="1" Margin="10,0,10,0" SelectedIndex="0" ItemsSource="{StaticResource ComboBoxOptions}"/>
                                            <TextBlock Grid.Row="2" Text="TEXT" Margin="10" DockPanel.Dock="Left" VerticalAlignment="Center" FontWeight="Bold"/>
                                            <TextBox Name="SellerTrader_Flt_Input"  Grid.Row="3" Margin="10,0,10,0"/>

                                            <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Center">
                                                <Button Margin="10" Padding="5" Content="CANCEL" Click="ClosePopUp_Click" Style="{StaticResource cancelBtn}"/>
                                                <Button Padding="5" Margin="10" Background="#0094B3" Foreground="White" Content="APPLY" Click="SellerTrader_Filter_Apply" Style="{StaticResource popUpBtn}"/>
                                            </StackPanel>
                                        </Grid>
                                    </Border>
                                </Popup>
                            </DockPanel>
                        </DataGridTextColumn.Header>
                    </DataGridTextColumn>
                    <DataGridTextColumn  Width="auto" Binding="{Binding Path=SellerTradeRegistrant}">
                        <DataGridTextColumn.HeaderStyle>
                            <Style TargetType="{x:Type DataGridColumnHeader}">
                                <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                            </Style>
                        </DataGridTextColumn.HeaderStyle>
                        <DataGridTextColumn.Header>
                            <DockPanel>
                                <TextBlock Style="{StaticResource SortDirectionStyle}">
                                    <TextBlock.Triggers></TextBlock.Triggers>
                                </TextBlock>
                                <TextBlock Text="Seller Trade Registrant" Margin="10,5" />
                                <ToggleButton Name="SellerTradeRegistrant" Click="BtnFilter_Click" Style="{Binding Source={StaticResource DropDown_btn}}" />
                                <Popup Name="pop_CheckboxFlt_SellerTradeRegistrant" Closed="ClosePopUp" Style="{Binding Source={StaticResource PopUpStyle}}">
                                    <Border Background="White" BorderBrush="Gray" BorderThickness="1">
                                        <Grid>
                                            <Grid.RowDefinitions>
                                                <RowDefinition />
                                                <RowDefinition />
                                                <RowDefinition />
                                                <RowDefinition />
                                            </Grid.RowDefinitions>
                                            <DockPanel Grid.Row="0" Margin="10">
                                                <!--<TextBlock Text="SEARCH" Margin="10" DockPanel.Dock="Left" VerticalAlignment="Center" FontWeight="Bold"/>
                                                <TextBox x:Name="tbFilter_SellerTradeRegistrant" Margin="10" Tag="SellerTradeRegistrant" Width="Auto"/>-->
                                            </DockPanel>
                                            <StackPanel Orientation="Vertical" Grid.Row="1" Grid.RowSpan="2">
                                                <CheckBox Grid.Row="1" Name="Checkbox_selectAll_SellerTradeRegistrant" Margin="10,0,0,0" Tag="{Binding}"  IsChecked="True" Checked="SelectAllCheckBox_Checked" Unchecked="SelectAllCheckBox_Checked">(Select All)</CheckBox>
                                                <ListBox Grid.Row="2" Margin="10" ItemsSource="{Binding}" Style="{Binding Source={StaticResource FilterListbox_Style}}" />
                                            </StackPanel>
                                            <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Center">
                                                <Button Margin="10" Padding="5" Content="CANCEL" Click="ClosePopUp_Click" Style="{StaticResource cancelBtn}"/>
                                                <Button Padding="5" Margin="10" Background="#0094B3" Foreground="White" Content="APPLY" Style="{StaticResource popUpBtn}"
                                                    Click="SellerTradeRegistrant_Filter_Apply"/>
                                            </StackPanel>
                                        </Grid>
                                    </Border>
                                </Popup>
                            </DockPanel>
                        </DataGridTextColumn.Header>
                    </DataGridTextColumn>
                    <DataGridTextColumn Width="auto" Binding="{Binding Path=SellerAccount}">
                        <DataGridTextColumn.HeaderStyle>
                            <Style TargetType="{x:Type DataGridColumnHeader}">
                                <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                            </Style>
                        </DataGridTextColumn.HeaderStyle>
                        <DataGridTextColumn.Header>
                            <DockPanel>
                                <TextBlock Style="{StaticResource SortDirectionStyle}">
                                    <TextBlock.Triggers></TextBlock.Triggers>
                                </TextBlock>
                                <TextBlock Text="Seller Account" Margin="10,5" />
                                <ToggleButton  Name="SellerAccount" Style="{StaticResource DropDown_btn}" Click="BtnFilter_Click"/>
                                <Popup Name="pop_ComboFlt_SellerAccount" Style="{Binding Source={StaticResource PopUpStyle}}">
                                    <Border Background="White" BorderBrush="Gray" BorderThickness="1">
                                        <Grid>
                                            <Grid.RowDefinitions>
                                                <RowDefinition />
                                                <RowDefinition />
                                                <RowDefinition />
                                                <RowDefinition />
                                                <RowDefinition />
                                            </Grid.RowDefinitions>
                                            <TextBlock Text="VALUE" Margin="10"  VerticalAlignment="Center" FontWeight="Bold"/>
                                            <ComboBox Name="TextFltOptions_SellerAccount" Grid.Row="1" Margin="10,0,10,0" SelectedIndex="0" ItemsSource="{StaticResource ComboBoxOptions}"/>
                                            <TextBlock Grid.Row="2" Text="TEXT" Margin="10" DockPanel.Dock="Left" VerticalAlignment="Center" FontWeight="Bold"/>
                                            <TextBox Name="SellerAccount_Flt_Input"  Grid.Row="3" Margin="10,0,10,0"/>

                                            <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Center">
                                                <Button Margin="10" Padding="5" Content="CANCEL" Click="ClosePopUp_Click" Style="{StaticResource cancelBtn}"/>
                                                <Button Padding="5" Margin="10" Background="#0094B3" Foreground="White" Content="APPLY" Click="SellerAccount_Filter_Apply" Style="{StaticResource popUpBtn}"/>
                                            </StackPanel>
                                        </Grid>
                                    </Border>
                                </Popup>
                            </DockPanel>
                        </DataGridTextColumn.Header>
                    </DataGridTextColumn>
                    <DataGridTextColumn Width="auto" Binding="{Binding Path=SellerSecurID}">
                        <DataGridTextColumn.HeaderStyle>
                            <Style TargetType="{x:Type DataGridColumnHeader}">
                                <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                            </Style>
                        </DataGridTextColumn.HeaderStyle>
                        <DataGridTextColumn.Header>
                            <DockPanel>
                                <TextBlock Style="{StaticResource SortDirectionStyle}">
                                    <TextBlock.Triggers></TextBlock.Triggers>
                                </TextBlock>
                                <TextBlock Text="Seller Secur ID" Margin="10,5" />
                                <ToggleButton  Name="SellerSecurID" Style="{StaticResource DropDown_btn}" Click="BtnFilter_Click"/>
                                <Popup Name="pop_ComboFlt_SellerSecurID" Style="{Binding Source={StaticResource PopUpStyle}}">
                                    <Border Background="White" BorderBrush="Gray" BorderThickness="1">
                                        <Grid>
                                            <Grid.RowDefinitions>
                                                <RowDefinition />
                                                <RowDefinition />
                                                <RowDefinition />
                                                <RowDefinition />
                                                <RowDefinition />
                                            </Grid.RowDefinitions>
                                            <TextBlock Text="VALUE" Margin="10"  VerticalAlignment="Center" FontWeight="Bold"/>
                                            <ComboBox Name="TextFltOptions_SellerSecurID" Grid.Row="1" Margin="10,0,10,0" SelectedIndex="0" ItemsSource="{StaticResource ComboBoxOptions}"/>
                                            <TextBlock Grid.Row="2" Text="TEXT" Margin="10" DockPanel.Dock="Left" VerticalAlignment="Center" FontWeight="Bold"/>
                                            <TextBox Name="SellerSecurID_Flt_Input"  Grid.Row="3" Margin="10,0,10,0"/>

                                            <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Center">
                                                <Button Margin="10" Padding="5" Content="CANCEL" Click="ClosePopUp_Click" Style="{StaticResource cancelBtn}"/>
                                                <Button Padding="5" Margin="10" Background="#0094B3" Foreground="White" Content="APPLY" Click="SellerSecurID_Filter_Apply" Style="{StaticResource popUpBtn}"/>
                                            </StackPanel>
                                        </Grid>
                                    </Border>
                                </Popup>
                            </DockPanel>
                        </DataGridTextColumn.Header>
                    </DataGridTextColumn>
                    <DataGridTextColumn  Width="auto" Binding="{Binding Path=SellerBroker}">
                        <DataGridTextColumn.HeaderStyle>
                            <Style TargetType="{x:Type DataGridColumnHeader}">
                                <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                            </Style>
                        </DataGridTextColumn.HeaderStyle>
                        <DataGridTextColumn.Header>
                            <DockPanel>
                                <TextBlock Style="{StaticResource SortDirectionStyle}">
                                    <TextBlock.Triggers></TextBlock.Triggers>
                                </TextBlock>
                                <TextBlock Text="Seller Broker" Margin="10,5" />
                                <ToggleButton Name="SellerBroker" Click="BtnFilter_Click" Style="{Binding Source={StaticResource DropDown_btn}}" />
                                <Popup Name="pop_CheckboxFlt_SellerBroker" Closed="ClosePopUp" Style="{Binding Source={StaticResource PopUpStyle}}">
                                    <Border Background="White" BorderBrush="Gray" BorderThickness="1">
                                        <Grid>
                                            <Grid.RowDefinitions>
                                                <RowDefinition />
                                                <RowDefinition />
                                                <RowDefinition />
                                                <RowDefinition />
                                            </Grid.RowDefinitions>
                                            <DockPanel Grid.Row="0" Margin="10">
                                                <!--<TextBlock Text="SEARCH" Margin="10" DockPanel.Dock="Left" VerticalAlignment="Center" FontWeight="Bold"/>
                                                <TextBox x:Name="tbFilter_SellerBroker" Margin="10" Tag="SellerBroker" Width="Auto"/>-->
                                            </DockPanel>
                                            <StackPanel Orientation="Vertical" Grid.Row="1" Grid.RowSpan="2">
                                                <CheckBox Grid.Row="1" Name="Checkbox_selectAll_SellerBroker" Margin="10,0,0,0" Tag="{Binding}"  IsChecked="True" Checked="SelectAllCheckBox_Checked" Unchecked="SelectAllCheckBox_Checked">(Select All)</CheckBox>
                                                <ListBox Grid.Row="2" Margin="10" ItemsSource="{Binding}" Style="{Binding Source={StaticResource FilterListbox_Style}}" />
                                            </StackPanel>
                                            <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Center">
                                                <Button Margin="10" Padding="5" Content="CANCEL" Click="ClosePopUp_Click" Style="{StaticResource cancelBtn}" />
                                                <Button Padding="5" Margin="10" Background="#0094B3" Foreground="White" Content="APPLY" Style="{StaticResource popUpBtn}"
                                                    Click="SellerBroker_Filter_Apply"/>
                                            </StackPanel>
                                        </Grid>
                                    </Border>
                                </Popup>
                            </DockPanel>
                        </DataGridTextColumn.Header>
                    </DataGridTextColumn>
                    <DataGridTextColumn Width="auto" Binding="{Binding Path=SellerComment}">
                        <DataGridTextColumn.HeaderStyle>
                            <Style TargetType="{x:Type DataGridColumnHeader}">
                                <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                            </Style>
                        </DataGridTextColumn.HeaderStyle>
                        <DataGridTextColumn.Header>
                            <DockPanel>
                                <TextBlock Style="{StaticResource SortDirectionStyle}">
                                    <TextBlock.Triggers></TextBlock.Triggers>
                                </TextBlock>
                                <TextBlock Text="Seller Comment" Margin="10,5" />
                                <ToggleButton  Name="SellerComment" Style="{StaticResource DropDown_btn}" Click="BtnFilter_Click"/>
                                <Popup Name="pop_ComboFlt_SellerComment" Style="{Binding Source={StaticResource PopUpStyle}}">
                                    <Border Background="White" BorderBrush="Gray" BorderThickness="1">
                                        <Grid>
                                            <Grid.RowDefinitions>
                                                <RowDefinition />
                                                <RowDefinition />
                                                <RowDefinition />
                                                <RowDefinition />
                                                <RowDefinition />
                                            </Grid.RowDefinitions>
                                            <TextBlock Text="VALUE" Margin="10"  VerticalAlignment="Center" FontWeight="Bold"/>
                                            <ComboBox Name="TextFltOptions_SellerComment" Grid.Row="1" Margin="10,0,10,0" SelectedIndex="0" ItemsSource="{StaticResource ComboBoxOptions}"/>
                                            <TextBlock Grid.Row="2" Text="TEXT" Margin="10" DockPanel.Dock="Left" VerticalAlignment="Center" FontWeight="Bold"/>
                                            <TextBox Name="SellerComment_Flt_Input"  Grid.Row="3" Margin="10,0,10,0"/>

                                            <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Center">
                                                <Button Margin="10" Padding="5" Content="CANCEL" Click="ClosePopUp_Click" Style="{StaticResource cancelBtn}" />
                                                <Button Padding="5" Margin="10" Background="#0094B3" Foreground="White" Content="APPLY" Click="SellerComment_Filter_Apply" Style="{StaticResource popUpBtn}"/>
                                            </StackPanel>
                                        </Grid>
                                    </Border>
                                </Popup>
                            </DockPanel>
                        </DataGridTextColumn.Header>
                    </DataGridTextColumn>
                    <DataGridTextColumn Width="auto" Binding="{Binding Path=SellerInternalTrdID}">
                        <DataGridTextColumn.HeaderStyle>
                            <Style TargetType="{x:Type DataGridColumnHeader}">
                                <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                            </Style>
                        </DataGridTextColumn.HeaderStyle>
                        <DataGridTextColumn.Header>
                            <DockPanel>
                                <TextBlock Style="{StaticResource SortDirectionStyle}">
                                    <TextBlock.Triggers></TextBlock.Triggers>
                                </TextBlock>
                                <TextBlock Text="Seller Internal Trade ID" Margin="10,5" />
                                <ToggleButton  Name="SellerInternalTrdID" Style="{StaticResource DropDown_btn}" Click="BtnFilter_Click"/>
                                <Popup Name="pop_ComboFlt_SellerInternalTrdID" Style="{Binding Source={StaticResource PopUpStyle}}">
                                    <Border Background="White" BorderBrush="Gray" BorderThickness="1">
                                        <Grid>
                                            <Grid.RowDefinitions>
                                                <RowDefinition />
                                                <RowDefinition />
                                                <RowDefinition />
                                                <RowDefinition />
                                                <RowDefinition />
                                            </Grid.RowDefinitions>
                                            <TextBlock Text="VALUE" Margin="10"  VerticalAlignment="Center" FontWeight="Bold"/>
                                            <ComboBox Name="TextFltOptions_SellerInternalTrdID" Grid.Row="1" Margin="10,0,10,0" SelectedIndex="0" ItemsSource="{StaticResource ComboBoxOptions}"/>
                                            <TextBlock Grid.Row="2" Text="TEXT" Margin="10" DockPanel.Dock="Left" VerticalAlignment="Center" FontWeight="Bold"/>
                                            <TextBox Name="SellerInternalTrdID_Flt_Input"  Grid.Row="3" Margin="10,0,10,0"/>

                                            <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Center">
                                                <Button Margin="10" Padding="5" Content="CANCEL" Click="ClosePopUp_Click" Style="{StaticResource cancelBtn}" />
                                                <Button Padding="5" Margin="10" Background="#0094B3" Foreground="White" Content="APPLY" Click="SellerInternalTrdID_Filter_Apply" Style="{StaticResource popUpBtn}"/>
                                            </StackPanel>
                                        </Grid>
                                    </Border>
                                </Popup>
                            </DockPanel>
                        </DataGridTextColumn.Header>
                    </DataGridTextColumn>
                    <DataGridTextColumn Width="auto" Binding="{Binding Path=SellSource}">
                        <DataGridTextColumn.HeaderStyle>
                            <Style TargetType="{x:Type DataGridColumnHeader}">
                                <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                            </Style>
                        </DataGridTextColumn.HeaderStyle>
                        <DataGridTextColumn.Header>
                            <DockPanel>
                                <TextBlock Style="{StaticResource SortDirectionStyle}">
                                    <TextBlock.Triggers></TextBlock.Triggers>
                                </TextBlock>
                                <TextBlock Text="Sell Source" Margin="10,5" />
                                <ToggleButton  Name="SellSource" Style="{StaticResource DropDown_btn}" Click="BtnFilter_Click"/>
                                <Popup Name="pop_ComboFlt_SellSource" Style="{Binding Source={StaticResource PopUpStyle}}">
                                    <Border Background="White" BorderBrush="Gray" BorderThickness="1">
                                        <Grid>
                                            <Grid.RowDefinitions>
                                                <RowDefinition />
                                                <RowDefinition />
                                                <RowDefinition />
                                                <RowDefinition />
                                                <RowDefinition />
                                            </Grid.RowDefinitions>
                                            <TextBlock Text="VALUE" Margin="10"  VerticalAlignment="Center" FontWeight="Bold"/>
                                            <ComboBox Name="TextFltOptions_SellSource" Grid.Row="1" Margin="10,0,10,0" SelectedIndex="0" ItemsSource="{StaticResource ComboBoxOptions}"/>
                                            <TextBlock Grid.Row="2" Text="TEXT" Margin="10" DockPanel.Dock="Left" VerticalAlignment="Center" FontWeight="Bold"/>
                                            <TextBox Name="SellSource_Flt_Input"  Grid.Row="3" Margin="10,0,10,0"/>

                                            <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Center">
                                                <Button Margin="10" Padding="5" Content="CANCEL" Click="ClosePopUp_Click" Style="{StaticResource cancelBtn}" />
                                                <Button Padding="5" Margin="10" Background="#0094B3" Foreground="White" Content="APPLY" Click="SellSource_Filter_Apply" Style="{StaticResource popUpBtn}"/>
                                            </StackPanel>
                                        </Grid>
                                    </Border>
                                </Popup>
                            </DockPanel>
                        </DataGridTextColumn.Header>
                    </DataGridTextColumn>
                    <DataGridTextColumn Width="auto" Binding="{Binding Path=USI}">
                        <DataGridTextColumn.HeaderStyle>
                            <Style TargetType="{x:Type DataGridColumnHeader}">
                                <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                            </Style>
                        </DataGridTextColumn.HeaderStyle>
                        <DataGridTextColumn.Header>
                            <DockPanel>
                                <TextBlock Style="{StaticResource SortDirectionStyle}">
                                    <TextBlock.Triggers></TextBlock.Triggers>
                                </TextBlock>
                                <TextBlock Text="USI" Margin="10,5" />
                                <ToggleButton  Name="USI" Style="{StaticResource DropDown_btn}" Click="BtnFilter_Click"/>
                                <Popup Name="pop_ComboFlt_USI" Style="{Binding Source={StaticResource PopUpStyle}}">
                                    <Border Background="White" BorderBrush="Gray" BorderThickness="1">
                                        <Grid>
                                            <Grid.RowDefinitions>
                                                <RowDefinition />
                                                <RowDefinition />
                                                <RowDefinition />
                                                <RowDefinition />
                                                <RowDefinition />
                                            </Grid.RowDefinitions>
                                            <TextBlock Text="VALUE" Margin="10"  VerticalAlignment="Center" FontWeight="Bold"/>
                                            <ComboBox Name="TextFltOptions_USI" Grid.Row="1" Margin="10,0,10,0" SelectedIndex="0" ItemsSource="{StaticResource ComboBoxOptions}"/>
                                            <TextBlock Grid.Row="2" Text="TEXT" Margin="10" DockPanel.Dock="Left" VerticalAlignment="Center" FontWeight="Bold"/>
                                            <TextBox Name="USI_Flt_Input"  Grid.Row="3" Margin="10,0,10,0"/>

                                            <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Center">
                                                <Button Margin="10" Padding="5" Content="CANCEL" Click="ClosePopUp_Click" Style="{StaticResource cancelBtn}" />
                                                <Button Padding="5" Margin="10" Background="#0094B3" Foreground="White" Content="APPLY" Click="USI_Filter_Apply" Style="{StaticResource popUpBtn}"/>
                                            </StackPanel>
                                        </Grid>
                                    </Border>
                                </Popup>
                            </DockPanel>
                        </DataGridTextColumn.Header>
                    </DataGridTextColumn>
                    <DataGridTextColumn  Width="auto" Binding="{Binding Path=TradingSessionSubID}">
                        <DataGridTextColumn.HeaderStyle>
                            <Style TargetType="{x:Type DataGridColumnHeader}">
                                <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                            </Style>
                        </DataGridTextColumn.HeaderStyle>
                        <DataGridTextColumn.Header>
                            <DockPanel>
                                <TextBlock Style="{StaticResource SortDirectionStyle}">
                                    <TextBlock.Triggers></TextBlock.Triggers>
                                </TextBlock>
                                <TextBlock Text="Trading Session" Margin="10,5" />
                                <ToggleButton Name="TradingSessionSubID" Click="BtnFilter_Click" Style="{Binding Source={StaticResource DropDown_btn}}" />
                                <Popup Name="pop_CheckboxFlt_TradingSessionSubID" Closed="ClosePopUp" Style="{Binding Source={StaticResource PopUpStyle}}">
                                    <Border Background="White" BorderBrush="Gray" BorderThickness="1">
                                        <Grid>
                                            <Grid.RowDefinitions>
                                                <RowDefinition />
                                                <RowDefinition />
                                                <RowDefinition />
                                                <RowDefinition />
                                            </Grid.RowDefinitions>
                                            <DockPanel Grid.Row="0" Margin="10">
                                                <!--<TextBlock Text="SEARCH" Margin="10" DockPanel.Dock="Left" VerticalAlignment="Center" FontWeight="Bold"/>
                                                <TextBox x:Name="tbFilter_TradingSessionSubID" Margin="10" Tag="TradingSessionSubID" Width="Auto"/>-->
                                            </DockPanel>
                                            <StackPanel Orientation="Vertical" Grid.Row="1" Grid.RowSpan="2">
                                                <CheckBox Grid.Row="1" Name="Checkbox_selectAll_TradingSessionSubID" Margin="10,0,0,0" Tag="{Binding}"  IsChecked="True" Checked="SelectAllCheckBox_Checked" Unchecked="SelectAllCheckBox_Checked">(Select All)</CheckBox>
                                                <ListBox Grid.Row="2" Margin="10" ItemsSource="{Binding}" Style="{Binding Source={StaticResource FilterListbox_Style}}" />
                                            </StackPanel>
                                            <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Center">
                                                <Button Margin="10" Padding="5" Content="CANCEL" Click="ClosePopUp_Click" Style="{StaticResource cancelBtn}"/>
                                                <Button Padding="5" Margin="10" Background="#0094B3" Foreground="White" Content="APPLY" Style="{StaticResource popUpBtn}"
                                                    Click="TrdSessionSubID_Filter_Apply"/>
                                            </StackPanel>
                                        </Grid>
                                    </Border>
                                </Popup>
                            </DockPanel>
                        </DataGridTextColumn.Header>
                    </DataGridTextColumn>
                    <DataGridTextColumn  Width="auto" Binding="{Binding Path=QtyInUnits}">
                        <DataGridTextColumn.HeaderStyle>
                            <Style TargetType="{x:Type DataGridColumnHeader}">
                                <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                            </Style>
                        </DataGridTextColumn.HeaderStyle>
                        <DataGridTextColumn.Header>
                            <DockPanel>
                                <TextBlock Style="{StaticResource SortDirectionStyle}">
                                    <TextBlock.Triggers></TextBlock.Triggers>
                                </TextBlock>
                                <TextBlock Text="Quantity In Units" Margin="10,5" />
                                <ToggleButton Name="QtyInUnits"  Click="BtnFilter_Click" Style="{Binding Source={StaticResource DropDown_btn}}"/>
                                <Popup x:Name="pop_RangeFlt_QtyInUnits" Style="{Binding Source={StaticResource PopUpStyle}}">
                                    <Border Background="White" BorderBrush="Gray" BorderThickness="1">
                                        <Grid>
                                            <Grid.RowDefinitions>
                                                <RowDefinition Height="1*" />
                                                <RowDefinition Height="2*"  />
                                                <RowDefinition Height="1*" />
                                                <RowDefinition Height="2*" />
                                                <RowDefinition Height="1*" />
                                            </Grid.RowDefinitions>

                                            <TextBlock Text="Minimum" Margin="10" Foreground="Black"/>
                                            <Grid Grid.Row="1" VerticalAlignment="Top" HorizontalAlignment="Stretch" Margin="10,0,10,0">
                                                <Grid.RowDefinitions>
                                                    <RowDefinition />
                                                    <RowDefinition />
                                                </Grid.RowDefinitions>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="3*" />
                                                    <ColumnDefinition Width="1*" />
                                                </Grid.ColumnDefinitions>
                                                <TextBox x:Name="MinNum_QtyInUnits" Text="" Grid.Row="0" Grid.RowSpan="2" HorizontalAlignment="Stretch"/>
                                                <RepeatButton  Grid.Column="1" Grid.Row="0" FontSize="8" Content="&#xf0de;" FontFamily="{StaticResource FontAwesome}" 
                                      VerticalContentAlignment="Center" HorizontalContentAlignment="Center" Click="NUDButtonUP_Click" />
                                                <RepeatButton  Grid.Column="1" Grid.Row="1" FontSize="8" Content="&#xf0dd;" FontFamily="{StaticResource FontAwesome}" 
                                      VerticalContentAlignment="Center" HorizontalContentAlignment="Center" Height="13" VerticalAlignment="Bottom" Click="NUDButtonDown_Click">
                                                    <RepeatButton.Style>
                                                        <Style TargetType="{x:Type RepeatButton}">
                                                            <Style.Triggers>
                                                                <DataTrigger Binding="{Binding Text, ElementName=MinNum_QtyInUnits, UpdateSourceTrigger=PropertyChanged}" Value="0">
                                                                    <Setter Property="IsEnabled" Value="False"/>
                                                                </DataTrigger>
                                                                <DataTrigger Binding="{Binding Text, ElementName=MinNum_QtyInUnits, UpdateSourceTrigger=PropertyChanged}" Value="">
                                                                    <Setter Property="IsEnabled" Value="False"/>
                                                                </DataTrigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </RepeatButton.Style>
                                                </RepeatButton>
                                            </Grid>

                                            <TextBlock Grid.Row="2" Text="Maximum" Margin="10" Foreground="Black" />
                                            <Grid Grid.Row="3" VerticalAlignment="Top" HorizontalAlignment="Stretch" Margin="10,0,10,0">
                                                <Grid.RowDefinitions>
                                                    <RowDefinition />
                                                    <RowDefinition />
                                                </Grid.RowDefinitions>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="3*" />
                                                    <ColumnDefinition Width="1*" />
                                                </Grid.ColumnDefinitions>
                                                <TextBox x:Name="MaxNum_QtyInUnits" Text="" Grid.RowSpan="2" HorizontalAlignment="Stretch" />
                                                <RepeatButton Grid.Column="1" Grid.Row="0" FontSize="8" Content="&#xf0de;" FontFamily="{StaticResource FontAwesome}"  
                                                          VerticalContentAlignment="Center" HorizontalContentAlignment="Center" Click="NUDButtonUP_Click"/>
                                                <RepeatButton Grid.Column="1" Grid.Row="1" FontSize="8"  Content="&#xf0dd;" FontFamily="{StaticResource FontAwesome}" 
                                                          VerticalContentAlignment="Center" HorizontalContentAlignment="Center" Height="13" VerticalAlignment="Bottom" Click="NUDButtonDown_Click">
                                                    <RepeatButton.Style>
                                                        <Style TargetType="{x:Type RepeatButton}">
                                                            <Style.Triggers>
                                                                <DataTrigger Binding="{Binding Text, ElementName=MaxNum_QtyInUnits, UpdateSourceTrigger=PropertyChanged}" Value="0">
                                                                    <Setter Property="IsEnabled" Value="False"/>
                                                                </DataTrigger>
                                                                <DataTrigger Binding="{Binding Text, ElementName=MaxNum_QtyInUnits, UpdateSourceTrigger=PropertyChanged}" Value="">
                                                                    <Setter Property="IsEnabled" Value="False"/>
                                                                </DataTrigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </RepeatButton.Style>
                                                </RepeatButton>
                                            </Grid>
                                            <StackPanel  Orientation="Horizontal" HorizontalAlignment="Center" Grid.Row="4">
                                                <Button Content="CANCEL" Click="ClosePopUp_Click"  Margin="10" Padding="5" Style="{StaticResource cancelBtn}"/>
                                                <Button Name="popRangeFilter_btn" Tag="{Binding}" Margin="10" Padding="5" Background="#0094B3" Foreground="White"
                                                    Click="QtyInUnits_filter_Apply"  CommandParameter="" Content="APPLY" Style="{StaticResource popUpBtn}"/>
                                            </StackPanel>
                                        </Grid>
                                    </Border>
                                </Popup>
                            </DockPanel>
                        </DataGridTextColumn.Header>
                    </DataGridTextColumn>
                    <DataGridTextColumn  Width="auto" Binding="{Binding Path=QtyType}">
                        <DataGridTextColumn.HeaderStyle>
                            <Style TargetType="{x:Type DataGridColumnHeader}">
                                <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                            </Style>
                        </DataGridTextColumn.HeaderStyle>
                        <DataGridTextColumn.Header>
                            <DockPanel>
                                <TextBlock Style="{StaticResource SortDirectionStyle}">
                                    <TextBlock.Triggers></TextBlock.Triggers>
                                </TextBlock>
                                <TextBlock Text="Quantity Unit" Margin="10,5" />
                                <ToggleButton Name="QtyType" Click="BtnFilter_Click" Style="{Binding Source={StaticResource DropDown_btn}}" />
                                <Popup Name="pop_CheckboxFlt_QtyType" Closed="ClosePopUp" Style="{Binding Source={StaticResource PopUpStyle}}">
                                    <Border Background="White" BorderBrush="Gray" BorderThickness="1">
                                        <Grid>
                                            <Grid.RowDefinitions>
                                                <RowDefinition />
                                                <RowDefinition />
                                                <RowDefinition />
                                                <RowDefinition />
                                            </Grid.RowDefinitions>
                                            <DockPanel Grid.Row="0" Margin="10">
                                                <!--<TextBlock Text="SEARCH" Margin="10" DockPanel.Dock="Left" VerticalAlignment="Center" FontWeight="Bold"/>
                                                <TextBox x:Name="tbFilter_QtyType" Margin="10" Tag="QtyType" Width="Auto"/>-->
                                            </DockPanel>
                                            <StackPanel Orientation="Vertical" Grid.Row="1" Grid.RowSpan="2">
                                                <CheckBox Grid.Row="1" Name="Checkbox_selectAll_QtyType" Margin="10,0,0,0" Tag="{Binding}"  IsChecked="True" Checked="SelectAllCheckBox_Checked" Unchecked="SelectAllCheckBox_Checked">(Select All)</CheckBox>
                                                <ListBox Grid.Row="2" Margin="10" ItemsSource="{Binding}" Style="{Binding Source={StaticResource FilterListbox_Style}}" />
                                            </StackPanel>
                                            <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Center">
                                                <Button Margin="10" Padding="5" Content="CANCEL" Click="ClosePopUp_Click" Style="{StaticResource popUpBtn}"/>
                                                <Button Padding="5" Margin="10" Background="#0094B3" Foreground="White" Content="APPLY" Click="QtyType_Filter_Apply" Style="{StaticResource popUpBtn}"/>
                                            </StackPanel>
                                        </Grid>
                                    </Border>
                                </Popup>
                            </DockPanel>
                        </DataGridTextColumn.Header>
                    </DataGridTextColumn>
                </DataGrid.Columns>
            </DataGrid>
        </DockPanel>
    </Grid>
</UserControl>
